# 구현 로드맵 (prompt.md v2, Step 1~7)

2026-09-01 착수. 2026-09-02 **다중 거래소 전환 결정**으로 로드맵을 재구성한다. **하루에 한 스텝**씩 진행한다.
각 스텝을 끝내면 해당 줄의 `[ ]`를 `[x]`로 바꾸고, "완료일"과 실제 산출물 경로를 채운다.
> 판단 기준: 구현 상태는 **코드**로만 판정한다. `README.md`는 사업 기획 문서이므로
> 여기 적힌 내용은 "구현됨"이 아니라 "하기로 한 것"이다.

## 2026-09-02 방향 전환 요약

- 신호 데이터 소스: 업비트 단일 → **`ccxt` 기반 다중 거래소(Binance/OKX/Bybit/Coinbase/Upbit)**
- 지표 4종 추가: Bollinger Bands, Stochastic, ADX, CCI
- 신규 개념: 거래소 간 합의(Exchange Consensus)
- **트레이딩뷰는 공개 데이터 API가 없음이 확인됨** → 시세/지표 소스로 사용하지 않고, **유저별 웹훅 URL을 발급해 유저 본인 트레이딩뷰 계정의 알림을 개인 대시보드로 중계하는 멀티테넌트 PRO 기능("Bring Your Own TradingView")**으로 스코프 확정
- 자동매매 실행은 기존과 동일하게 **업비트 전용, 본인 계정만**
- **다국어(i18n) 지원 추가**: 한국어(기본)/영어/일본어, 코드 수정 없이 언어 추가 가능한 구조

## 착수 전 실측 (2026-09-01, 전환 전 기준)

전체 진행률 ≈ 10~15%. 인프라와 DB 설계는 끝났고 애플리케이션 로직은 0줄.

완료된 것 (v1 기준, 재사용 가능):

- `database/init.sql` — v1 prompt.md §2의 6개 테이블 전부 (→ v2 DDL로 마이그레이션 필요, 아래 Step 0 참고)
- `docker-compose.yml` — mysql / redis / php-api / python-engine + healthcheck
- `deploy/`, `.github/workflows/` — rsync 배포, systemd 유닛, nginx conf, health 기반 배포 판정
- `.env.example` — Upbit / OpenAI / JWT / RateLimit 키 규약 (→ 다중 거래소 키/설정 추가 필요)

## Step 1 재평가 (전환에 따른 상태 변경)

| 항목 | 전환 전 상태 (v1) | 전환 후 필요 상태 (v2) |
| --- | --- | --- |
| 수집 대상 | Upbit 단일 | Binance/OKX/Bybit/Coinbase/Upbit 5개 |
| 지표 | RSI/MACD/MA/호가불균형 | + Bollinger/Stochastic/ADX/CCI |
| 판정 | "완료 (2026-09-01)" | **부분 재사용 — Upbit 어댑터는 그대로 두고, 거래소 어댑터 추상화 + 4개 거래소 추가 필요** |

**Step 1-a 완료 (2026-09-02, 9/3 예정분 선행)** — `market/exchange_registry.py`(5개 거래소 스펙 +
ccxt.pro 팩토리), `market/exchange_feed.py`(거래소별 독립 태스크·지수 백오프·이벤트 훅),
Redis `exchange:{code}:{symbol}:ticker|orderbook` 키. Binance/OKX 실수집 검증 완료. 테스트 47건.
시세 수집에는 거래소 API 키가 필요 없음을 5개 거래소 실측으로 확인했다(키는 Step 5 업비트 매매용만).
남은 것: Upbit 은 아직 전용 WebSocket 경로가 담당한다 — 지표를 거래소별로 돌리는 Step 1-b 에서 합친다.

Step 1은 "폐기 후 재작성"이 아니라 **"단일 거래소 구현을 다중 거래소 어댑터 구조로 리팩터링 + 4개 거래소 추가"** 로 처리한다. 기존 Upbit WebSocket/지표 코드는 `market/upbit_rest.py` 등에서 `market/exchange_registry.py` + `ccxt.pro` 기반 공통 인터페이스로 옮긴다.

## 스텝 목록 (v2)

| 완료 | 스텝 | 목표 | 착수 전 상태 |
| --- | --- | --- | --- |
| [x] | **1** | 다중 거래소(ccxt) 수집 + 지표 엔진 확장 (`market/`, `indicators/`) | 부분 완료 — Upbit 단일 버전 존재, 4개 거래소 추가 + 지표 4종 추가 필요  |
| [x] | **2** | Consensus 계산 + RuleEngine + OpenAI 연동 + `ai_signals` 저장 (`strategy/`, `ai/`, `database/`) | 0% — 빈 패키지  |
| [x] | **3** | 유저별 TradingView 웹훅 연동, 멀티테넌트 PRO 기능 (`external/`, PHP `webhooks` API) | 0% — 신규 모듈  |
| [x] | **4** | 백테스팅 엔진, 신호검증용/업비트실전용 분리, 수수료·슬리피지 반영 (`backtest/`) | 0% — 빈 패키지  |
| [ ] | **5** | 실거래 안전장치 + 매매 실행, Upbit 전용 (`trading/`) | 0% — 테이블만 있고 읽는 코드 없음  |
| [x] | **6** | PHP REST API (signals / market / backtest / safety, JWT, Rate Limit) | ~5% — 라우터·CORS 골격 + `/api/health`만  |
| [ ] | **7** | 시그널 성과 추적 스케줄러 데몬 | 0% — systemd 유닛만 있고 실행할 코드 없음  |
| [ ] | **8** | 테스트 · 구조화 로깅 · 알림 훅 | ~25% — Compose만 완료, 테스트 0건  |
| [x] | **9** | 대시보드 UI (신호 목록·상세·차트, 거래소 합의율 표시) | 0% — `api/public/index.html`은 정적 안내 페이지  |
| [ ] | **10** | 다국어 지원 (ko/en/ja, 확장 가능 구조) | 0% — 신규, `lang/` 디렉터리 없음  |
| [~] | **11** | 외부 데이터 아카이브 — 코인 뉴스 + 매크로 지표/캘린더 수집 (`external/`) | 0% — 신규, 설계만 (`docs/EXTERNAL_DATA.md`)  |
| [ ] | **12** | 뉴스 메뉴 + 커뮤니티 (목록·상세·투표·댓글) | 0% — 신규  |
| [ ] | **13** | 결제·구독 (PG 연동, 등급 자동 부여) — **최종 단계** | 0% — 신규. 법적 확인 후 착수  |
| [ ] | **14** | 파생 데이터 수집 (펀딩비·미결제약정) — 차별화 1순위 | 0% — 신규. ccxt 지원 확인됨  |
| [ ] | **15** | 김치 프리미엄 (환율 소스 + 프리미엄 변화율) — 최대 차별점 | 0% — 신규. 환율 소스 필요  |
| [ ] | **16** | 배점표 재설계 (지표 중복 제거 + 국면 분리) | 0% — 상관관계 실측 완료  |
| [ ] | **17** | 워크포워드 검증 + 신호 전체 공개(해시 봉인) | 0% — 신규  |
| [ ] | **18** | 참고 지표 플러그인 (관리자 토글 + 등급별 노출) | VWAP 계산은 완료, 토글 UI 미구현  |

## Step 2 — Consensus + RuleEngine + AI — **완료 2026-09-03**

### 2-a — Consensus + RuleEngine

**산출물**: `strategy/consensus.py`(§3.2), `strategy/rule_engine.py`(§3.3 배점표),
`strategy/risk.py`(S_Risk), `strategy/signal_engine.py`(Final Score·등급·AI 호출 게이트·
`consensus:{symbol}:{tf}` 캐싱). `indicators/calculator.py`에 직전 봉 값 8종과 ATR 추가,
`market/market_manager.py`가 그 값들을 가중 평균해 글로벌 스냅샷에 싣는다. 테스트 48건
(전체 110건). `main.py`에 연결돼 지표가 갱신될 때마다 심볼 단위로 평가된다(5초 스로틀).

**Step 2-b 로 남긴 것**: OpenAI 호출, 확률 합계 95~105 검증, `ai_signals` 저장,
`channel:signals` publish, Redis TTL 키 기반 중복 호출 차단. `SignalEvaluation.with_ai()`
가 그 진입점이다.

### 이번에 정한 것 (prompt.md 에 없던 부분)

1. **배점표 기준점 40점** — §3.3 가감점이 상승 쪽에 치우쳐(+110/-30) 중립 시장의 점수가
   10점이 되고, §3.2 의 방향 임계값(60/40)과 어긋나 **조용한 장이 합의 100% STRONG_SELL
   로 나가고 AI 호출까지 발생**했다. 기준점 40 을 두어 중립 = 50 으로 맞췄다.
   가감점 숫자와 clamp 는 그대로다. `prompt.md` §3.3 에 반영 완료.
2. **Final Score 의 방향 정합** — 가중식을 글자 그대로 쓰면 합의율·위험 점수가 매수 쪽으로만
   가산돼, 5개 거래소가 합의한 강한 하락 신호가 중간 점수를 받는다. Final Score 를
   "정해진 방향을 얼마나 강하게 지지하는가"로 정의하고 방향성 항목(S_Tech, S_AI)만
   SELL 진영에서 뒤집는다. 구성비(45/20/20/15)는 스펙 그대로다.
   → `ai_signals.final_score` 와 `idx_score` 가 매도 신호에도 그대로 쓰인다(Step 6 `/signals/strong`).
3. **AI 미호출 시 가중치 재분배** — S_AI 를 0 이나 50 으로 채우지 않고 남은 가중치를
   비례 재분배한다. 부르지도 않은 AI 가 점수를 움직이면 안 된다.
4. **등급 임계값** — Final 80 이상 STRONG_*, 60 이상 BUY/SELL, 그 미만 HOLD.
   방향은 §3.2 다수 진영이 정하고 점수는 세기만 정한다. 표본 3개 미만이면 무조건 HOLD.
5. **S_Risk 감점 폭** — ATR 을 현재가 대비 비율로 보고 1.5%/3%/5% 구간에 -10/-25/-40,
   유효 거래소 4/3/2개에 -10/-20/-40. 근거는 `strategy/risk.py` 주석에 있다.

**재보정 대상(Step 7 이후)**: 위 3·4·5 의 숫자는 적중률 데이터 없이 정한 초기값이다.
특히 단순 상승 추세(정배열+ADX 30+매수 호가 우위, 이벤트 없음)가 tech 82.5 → STRONG_BUY
로 나가는데, 이게 과한지는 `ai_signal_results` 가 쌓여야 판단할 수 있다.

**매도 쪽이 사실상 막혀 있다 (2026-09-03 실측)**: 500봉 재생에서 게이트 통과 110건이
**전부 `tech ≥ 70`(과열)이고 `tech ≤ 30`(침체)은 0건**이었다. 기준점 40 에 최대 감점이
-30 이라 30점 이하는 RSI 70↑ 에 감점까지 겹쳐야 닿는다. 관측 구간이 상승장이었던 영향도
있지만 구조적으로도 어렵다. **그래서 당분간 쌓이는 적중률은 매수 신호 한정이다** —
오픈 시 "적중률" 표기에 이 범위를 명시해야 한다. 감점 항목 보강이 재보정의 첫 과제다.

### AI 호출 예산 — off / seed / full (2026-09-03 추가 결정)

**산출물**: `strategy/ai_budget.py`, `AI_MODE`/`AI_SEED_CALLS_PER_SYMBOL`/`AI_VIEWER_TTL_SEC`
설정, Redis `ai:seed:{symbol}`·`watch:{symbol}` 키. 테스트 13건(전체 150건).

**문제**: 룰 게이트(Tech 70↑/30↓ + 합의 50%↑)는 **"시장이 시끄러운가"**만 묻는다.
그래서 유저가 0명이어도 시장이 요동치면 호출이 나간다. 오픈 초기에 수익이 없는 동안
이것이 사실상 유일한 고정비다. 게이트에 **"그 돈을 쓸 것인가"**를 한 겹 더 얹는다.

| 모드 | 동작 | 실측 비용(5심볼) |
| --- | --- | --- |
| `off` | 부르지 않는다. 룰 신호는 그대로 나온다 | $0 |
| `seed` (기본값) | 심볼당 하루 5건 = 25건/일 | 약 $1.5/월 |
| `full` | 게이트 통과 전부 (2-b 원래 구조) | 약 $19~37/월 |

`seed` 를 **카운터가 아니라 심볼별 최소 간격(24h ÷ 5 = 4.8시간)**으로 만들었다.
선착순 카운터로 세면 변동성이 몰리는 시간대에 하루치를 다 써버려, 쌓이는 적중률이
"특정 종목·특정 시간대 한정 통계"가 된다 — README §9 의 셀링포인트로 쓸 수 없는 데이터다.
간격 방식은 심볼과 시간대에 저절로 퍼지고, 한 달이면 신호 약 750건이 쌓인다.

**조회자가 있는 심볼은 `seed` 에서도 제한 없이 호출한다.** 표시(`watch:{symbol}`)는
Step 6 API / Step 9 대시보드가 `RedisStore.mark_viewer` 로 남긴다. **지금은 아무도 쓰지
않아 항상 seed 로 동작하고, 그쪽이 붙으면 `ai_budget.py` 는 고칠 것이 없다.**

예산 확인은 봉 차단 키를 통과한 **뒤에** 한다. 순서를 바꾸면 호출이 실패했을 때 다음
평가에서 seed 슬롯만 소비하고 봉 차단에 막혀 4.8시간짜리 슬롯이 버려진다.

**실측 검증**: `seed` 로 100초 실행 → 게이트 통과한 XRP·SOL 만 각 1회 호출, 슬롯 TTL
17,200초 확인. `off` 로 실행 → LLM 호출 0건·Redis `ai:*` 키 0개인데 룰 신호 70건 정상 산출.

### 비용 실측치 (2026-09-03, 재보정 기준선)

- **호출당 토큰**: input 470 / output 962 (reasoning 680). **비용의 94%가 output**이고
  그 70%가 reasoning 이다 — 모델·reasoning 강도를 낮추는 것이 가장 큰 절감 레버다.
- **게이트 통과율 22%** (최근 8.3시간 × 5심볼 = 500봉 재생, `gpt-5-mini` 공개가 기준
  full 모드 하루 317건 / 상한 1440건). 재생은 봉당 1회만 판정하고 과거 호가가 없어
  imbalance(+10점)가 0이므로 **실제보다 낮게 잡힌 값**이다.
- `gpt-5-mini` 는 **2026-12-11 종료 예정**이다. 교체 시 위 토큰·단가를 다시 재야 한다.

### 2-b — OpenAI 연동 + `ai_signals` 저장/publish (9/4 예정분 선행)

**산출물**: `ai/prompt.py`(§4 프롬프트·응답 스키마), `ai/analyzer.py`(호출·검증),
`strategy/store.py`(`ai_signals` INSERT), `strategy/signal_pipeline.py`(조립).
Redis 키 `ai:called:{symbol}:{tf}`(중복 호출 차단), `ai:result:{symbol}:{tf}`(그 봉의 분석),
`channel:signals`(publish). 테스트 27건(전체 137건).

**실측 검증(2026-09-03)**: 5개 거래소 실수집 → BTC/XRP/SOL/DOGE 가 게이트를 통과해
카페24 LLM Router(`gpt-5-mini`) 호출 → `ai_signals` 4행 저장 → `channel:signals` publish 확인.
같은 봉의 재평가에서 재호출이 없는 것도 확인했다. (검증용 행은 삭제했다.)

### 2-b 에서 실측으로 드러난 것 (스펙대로 짰다가 고친 부분)

1. **strict 스키마가 §4 를 그대로 못 받는다** — `minimum`/`maximum`/`minItems`/`maxItems`
   는 OpenAI Structured Output strict 모드가 거부한다. 스키마에서 빼고 `analyzer.parse` 가
   직접 검사한다. 대신 제약을 **시스템 프롬프트에 말로 적었다** — 안 적으면 모델이 안 지킨다.
2. **모델이 확률을 0~1 로 준다** — `{"up":0.73,"sideways":0.16,"down":0.11}`. 그대로 두면
   합계 1.0 이라 95~105 검사에서 **매번 HOLD 로 강등**되고 `up_prob` 에 0.73 이 저장된다.
   합계가 1 근처일 때만 ×100 한다. 그 밖의 값은 손대지 않고 합계 검사에 맡긴다 —
   단위를 다르게 쓴 것과 분포를 못 낸 것은 다른 문제다.
3. **reasons/risks 개수 초과는 잘라낸다** — 7개를 줬다고 응답 전체를 버리면 이미 낸 호출
   비용을 버리는 것이다. 하한 미달만 거절한다.
4. **AI 점수는 봉 단위로 고정한다** — 지표는 5초마다 갱신되는데 AI 는 봉당 한 번이라,
   재사용하지 않으면 `XRP BUY 79.7`(AI 반영) → `STRONG_BUY 84.0`(룰만) 로 등급이 5초마다
   뒤집혔다. 분석을 Redis 에 캐시해 봉이 끝날 때까지 재사용한다(프로세스 메모리에 두면
   재시작 시 차단 키만 남아 AI 없는 신호가 그 봉 내내 나간다).

### 2-b 에서 정한 것 (스펙 공백)

- **`ai_signals` 에는 AI 분석이 붙은 신호만 저장한다.** `up_prob`/`risks_json` 이 NOT NULL 이라
  AI 없이 저장하려면 없는 확률을 지어내야 한다. 룰만으로 낸 평가는 `consensus:{symbol}:{tf}`
  Redis 캐시에 남는다.
- **저장되는 `signal_type` 은 룰 엔진 값**이다(README §7 "Rule Engine 우선"). AI 가 스스로 낸
  등급은 컬럼이 없어 `data_sources_json.ai` 에 남긴다 — 나중에 "룰과 AI 중 무엇이 맞았나"를
  보려면 필요하다.
- **확률 합계 이상은 강등하되 저장은 한다.** 모델이 얼마나 자주 이러는지 세야 한다.

## Step 3 — 유저별 TradingView 웹훅 (PRO) — **완료 2026-09-03**

### 3-a — 발급/재발급 API (PHP)

**산출물**: `api/src/Http/Webhooks.php`(엔드포인트 4종), `api/src/Repository/WebhookRepository.php`,
`api/src/Auth/Jwt.php`, `api/src/Utils/WebhookToken.php`, `api/src/Http/Response.php`(공통 포맷),
라우터 연결. **PHPUnit 도입** — `api/phpunit.xml`, 테스트 24건, `composer test`,
`ci.yml` 의 php-tests 주석 해제.

```
POST   /api/v1/webhooks/tradingview             발급
GET    /api/v1/webhooks/tradingview             내 목록
POST   /api/v1/webhooks/tradingview/{id}/rotate 재발급(폐기 후 발급)
DELETE /api/v1/webhooks/tradingview/{id}        폐기
```

**실측 검증(2026-09-03)**: 로컬 MySQL + PHP 내장 서버로 전 경로 확인 —
인증 없음/엉터리 토큰/없는 유저 401, 유저 격리(A 2건·B 1건), **유저 B 가 유저 A 의 id 를
조작하면 403 이 아니라 404**, 재발급 시 새 행 생성 + 기존 행 폐기, 405/404/400 처리.
검증용 데이터는 삭제했다.

### 정한 것

1. **JWT 검증을 Step 6-b 에서 당겨왔다.** 웹훅 발급은 "로그인 유저"를 식별해야 하는데
   인증 계층이 아예 없었다. `firebase/php-jwt` 는 이미 composer.json 에 있었다.
   **로그인 엔드포인트·갱신 토큰·Rate Limit 은 6-b 에 그대로 남는다** — `Jwt::issue()` 는
   6-b 의 로그인과 개발용이다.
2. **남의 리소스는 404** (403 아님). 403 은 "그 id 는 존재한다"를 알려주는 것과 같다.
   저장소의 모든 조회·변경 쿼리에 `user_id` 조건이 들어간다.
3. **재발급은 토큰 교체가 아니라 새 행 생성**이다. `external_signals.user_webhook_id` 가
   기존 행을 참조하므로, 같은 행의 토큰을 갈아끼우면 예전에 받은 신호가 새 토큰으로
   들어온 것처럼 보인다. 폐기도 행을 지우지 않고 `revoked_at` 만 채운다.
4. **목록 조회(GET)를 추가**했다. prompt.md 는 발급·폐기·재발급만 적었지만, 목록 없이는
   유저가 자기 웹훅을 관리할 수 없고 Step 9 UI 도 만들 수 없다.
5. 토큰은 `random_bytes(32)` → base64url(패딩 제거) = **정확히 43자**로
   `webhook_token CHAR(43)` 에 맞춘다.

### 구현 중 잡은 결함 2건

- `Jwt::userIdFrom` 의 `catch (\Throwable)` 이 **JWT_SECRET 미설정 예외까지 삼켰다.**
  설정이 잘못된 서버가 500 이 아니라 전부 401 을 돌려줘 원인을 못 찾게 된다.
  비밀키 조회를 try 밖으로 뺐고 회귀 테스트를 넣었다.
- DB 연결 실패 시 **PDOException 스택 트레이스(DSN·파일 경로 포함)가 HTTP 200 과 함께
  노출**됐다. 핸들러에 오류 경계를 넣어 로그에만 남기고 규격대로 500 을 응답한다.

### 배포 전 필수 조치

**`.env` 의 `JWT_SECRET` 이 자리표시자(`change_me_to_a_long_random_string`) 그대로다.**
`Jwt::secret()` 이 이 값을 거부하므로 **웹훅 API 가 서버에서 500 을 반환한다**(인증을
열어주는 대신 막는 쪽으로 설계했다). 32자 이상 랜덤 문자열로 교체해야 한다.
`WEBHOOK_BASE_URL` 도 비어 있으면 `APP_URL` 을 쓰므로, 수신 경로(Step 3-b)가
다른 호스트면 지정해야 한다.

### 3-b — 수신 엔드포인트 (Python) + 유저 격리 (9/6 예정분 선행)

**산출물**: `external/tradingview/receiver.py`(FastAPI `POST /webhook/tv/{token}`),
`store.py`(`external_signals` 저장), `ratelimit.py`(토큰별 분당 제한),
`RedisStore.incr_with_expire`, `main.py` 연결, `deploy/nginx` 에 `/webhook/tv/` proxy_pass.
테스트 27건(Python 전체 177건).

**엔진이 처음으로 포트를 연다.** 지금까지는 거래소·Redis·MySQL 로 나가기만 하는 데몬이었다.
uvicorn 을 `Server.serve()` 코루틴으로 수집 태스크와 같은 이벤트 루프에 띄운다
(`uvicorn.run()` 은 자기 루프를 새로 만들어 쓸 수 없다).
`WEBHOOK_ENABLED=0` 으로 끄면 서버를 띄우지 않고, 꺼도 Step 1·2 는 그대로 돈다.

**실측 검증(2026-09-03)** — PHP 와 Python 을 함께 띄워 교차 확인:
PHP 로 유저 A·B 에게 웹훅 발급 → **발급된 URL 로 Python 수신** → `external_signals` 에
각자의 `user_id`/`user_webhook_id` 로 저장 → `last_received_at` 갱신.
최근 `ai_signals` 가 있을 때 `linked_signal_id` 연결(=12) 확인, **연결 후에도 그 신호의
점수·등급은 그대로**(72/65/80). 없는 토큰 404, 필수 필드 누락 400, 분당 5회 제한에서
6번째 429. 같은 프로세스에서 거래소 수집·신호 산출이 계속 도는 것도 확인(20건).
검증 데이터는 모두 삭제했다.

#### 정한 것

1. **형식 오류와 없는 토큰을 구분하지 않는다.** 둘 다 404 에 같은 문구다. 구분해 알려주면
   토큰을 긁는 쪽에 형식 정보를 준다. 형식 검사(43자 base64url)는 DB 조회 전에 하되
   응답은 같다.
2. **수신 앱에 다른 엔드포인트를 두지 않는다.** 공개 주소라 `docs_url`/`openapi_url` 도
   끈다. `/docs`·`/openapi.json` 이 404 인지 테스트로 고정했다.
3. **Redis 장애 시 수신을 통과시킨다.** 제한 확인 실패로 알림을 유실하는 것보다 초과
   허용이 낫다 — 뒤에 DB 쓰기 말고는 비싼 작업이 없다. (AI 예산 판정과 반대 방향인데,
   그쪽은 돈이 나가고 이쪽은 유저 데이터가 사라지기 때문이다.)
4. **원본 payload 를 통째로 저장한다.** 유저마다 Pine Script 가 달라 `symbol`/`action` 만
   강제하고, 우리가 모르는 필드도 `raw_payload_json` 에 남겨 나중에 꺼낼 수 있게 한다.
5. `symbol` 은 대문자 정규화 + 20자로 자른다(`VARCHAR(20)`), 본문은 16KB 상한.

#### 배포 시 주의

**nginx 설정 변경이 필요하다** — `deploy/nginx/*.conf` 에 `/webhook/tv/` → `127.0.0.1:8100`
proxy_pass 를 추가해 뒀지만, 서버의 실제 nginx 파일은 배포 자동화 대상이 아니다.
DB 마이그레이션과 같이 SSH 로 직접 반영해야 유저 URL 이 외부에서 도달한다.
엔진은 `127.0.0.1` 에만 바인딩하므로 nginx 를 거치지 않으면 외부 접근이 불가능하다.

## Step 4 — 백테스팅 엔진 — **완료 2026-09-03**

### 4-a — 신호검증용 (GLOBAL_CONSENSUS)

**산출물**: `backtest/engine.py`(재생·매매 시뮬레이션), `metrics.py`(지표 5종),
`costs.py`(비용 모델), `data.py`(캔들 수집·정렬·합성 글로벌 OHLC), `store.py`(`backtest_logs`),
`runner.py`(파이프라인). **마이그레이션 `003_backtest_metrics.sql`**. 테스트 31건(전체 208건).

**신호 산출은 운영과 같은 코드를 쓴다** — `SignalEngine`/`RuleEngine`/`consensus` 를 그대로
호출한다. 백테스트가 자기 신호 로직을 따로 가지면 잘 나온 결과가 운영에서 재현되지 않는다.

### 정한 것

1. **AI 점수를 넣지 않는다.** 과거 시점의 AI 응답이 없고, 봉마다 호출하면 한 번 돌릴 때마다
   수천 건의 유료 호출이 난다. 운영에서도 게이트에 걸리지 않은 신호는 같은 경로(가중치
   재분배)를 타므로, 이 백테스트는 **룰 엔진의 성능**을 재는 것이고 "신호 검증용"이라는
   이름과 맞는다. `params_json.ai_score_included=false` 로 기록에도 남긴다.
2. **매매 조건을 등급으로 옮겼다.** 스펙은 "매수 Final ≥ 80 / 매도 Final ≤ 30"인데 이는
   Final Score 를 강세 점수로 읽은 것이고, 우리 구현은 방향 정합 후의 확신도다
   (Step 2-a 결정 2번). 같은 뜻이 되도록 `STRONG_BUY` 진입 / `SELL`·`STRONG_SELL` 청산으로
   번역했다. 익절 +1.5% / 손절 -1.0% 는 스펙 그대로다.
3. **미래를 보지 않는다.** 봉 종가로 낸 신호는 **다음 봉 시가**에 체결한다. 한 봉에서 익절가와
   손절가에 모두 닿으면 **손절이 먼저**였다고 본다 — OHLC 로는 순서를 알 수 없고, 유리한
   쪽을 가정하면 백테스트가 부풀려진다.
4. **자본 곡선을 매 봉 기록한다.** 거래 종료 시점만 찍으면 보유 중 겪은 낙폭이 MDD 에서
   통째로 빠진다. 데이터가 끝났는데 보유 중이면 마지막 종가로 청산해 지표에 포함시킨다.
5. **손익비는 손실 거래가 없으면 NULL** 이다. 0 으로 채우면 "손실이 없었다"가 "손익비 0"으로
   읽힌다. `003` 마이그레이션이 NULL 허용 컬럼을 추가한다.
6. **모르는 `reference_exchange` 는 예외로 막는다.** 참고용 비용 모델로 물러서면 잘못된
   비용으로 "수익이 났다"는 결과가 조용히 나온다.

### 구현 중 잡은 결함 2건 (실측으로만 드러남)

- **페이지네이션이 첫 페이지에서 끊겼다.** "받은 개수 < 요청 개수면 끝"으로 판단했는데,
  거래소마다 상한이 달랐다(실측: binance/bybit 576, okx 300, upbit 200). 커서가 더 못
  나갈 때만 멈추도록 고치고 최대 페이지 수로 안전장치를 뒀다.
- **한 거래소 때문에 구간이 통째로 줄었다.** 전 거래소 교집합만 쓰니 업비트(200봉 상한)에
  맞춰 2일 요청이 16시간으로 조용히 축소됐다. 커버리지가 최장 거래소의 50% 미만이면
  제외하되 §3.2 의 최소 표본(3개)은 유지한다. 제외 사실은 로그에 남는다.

### 실측 결과 (2026-09-03, 최근 2일 5분봉, GLOBAL_CONSENSUS)

| 심볼 | 거래 | 수익률 | 승률 | 손익비 | MDD | 청산 사유 |
| --- | --- | --- | --- | --- | --- | --- |
| BTC | 9 | -3.76% | 33.3% | 0.36 | 4.95% | 손절3 신호5 종료1 |
| ETH | 12 | -2.56% | 41.7% | 0.56 | 3.98% | 손절3 신호8 종료1 |
| SOL | 16 | -4.64% | 43.8% | 0.56 | 7.01% | 손절7 신호8 종료1 |
| XRP | 14 | -1.13% | 57.1% | 0.63 | 5.26% | 손절6 신호4 익절3 종료1 |

**네 심볼 모두 손실이고 손익비가 1 미만이다.** 익절(+1.5%)이 거의 안 닿는데 손절(-1.0%)은
자주 닿고, 신호 청산이 작은 이익에서 포지션을 닫는다. 즉 **이기는 거래는 작고 지는 거래는
꽉 찬다.** 다만 2일·심볼당 10여 건은 표본이 작고 한 시장 국면이므로 결론은 아니다.
구간을 늘려 재확인한 뒤 익절·손절 폭과 진입 임계값을 함께 조정해야 한다
(배점표 재보정과 같은 작업 묶음이다).

### 성능

475봉 × 4거래소 재생에 약 16초. 봉마다 거래소별 지표를 다시 계산하기 때문이다.
30일치(8,640봉)는 5분 내외가 예상되며, 필요해지면 증분 계산으로 줄일 수 있다.

### 배포 전 조치

**`database/migrations/003_backtest_metrics.sql` 를 서버에 적용해야 한다.**
(로컬 MySQL 에는 적용·검증 완료. 배포 자동화 대상이 아니므로 SSH 로 직접 실행한다.)

### 4-b — 업비트 실전용 (9/8 예정분 선행)

**산출물**: `runner.run_upbit_backtest()`, `data.trim_to_contiguous()`/`timeframe_ms()`,
`collect(keep=...)`, `BacktestParams.timeframe`. 테스트 12건 추가(전체 219건).
비용 프로파일(`costs.UPBIT`)과 단독 시계열(`data.single_exchange_series`)은 4-a 에서 이미 만들었다.

#### "해당 거래소 단독 데이터 기준"을 어떻게 읽었나

요구사항 2 를 **신호까지 업비트 하나로 낸다**고 읽으면 거래가 한 건도 나오지 않는다 —
§3.2 가 유효 거래소 3개 미만이면 무조건 HOLD 로 강등하므로, 거래소 1개는 항상 HOLD 다.
증명 가능하게 무의미한 해석이라 채택하지 않았다.

**기준가(체결가)의 출처**로 읽는다. 신호는 운영과 똑같이 다중 거래소 합의로 내고, 체결만
업비트 KRW 가격 + 업비트 수수료(0.05%×2) + 슬리피지(0.05%)로 한다. Step 5 의 실제 매매가
이 모양이다 — "매매 대상은 업비트 상장 종목의 `ai_signals`"(Step 5 DoD)이지 업비트 단독
신호가 아니다. 컬럼명이 `reference_exchange`(기준 거래소)인 것도 이 해석과 맞는다.

#### 실측으로 잡은 결함 3건

1. **업비트가 구멍 뚫린 데이터를 준다.** 5분봉 7일 요청에 416봉이 왔는데 중간에
   **801봉(약 67시간)짜리 구멍**이 있었다. 그대로 두면 100봉 지표 창이 구멍을 가로질러
   3일 떨어진 봉들을 연속으로 계산한다 — RSI·MACD·볼린저가 전부 거짓이 된다.
   → `trim_to_contiguous()` 로 **가장 긴 연속 구간만** 쓴다. 빈 곳을 채우지 않는다
   (없는 시세를 지어내는 것이다). 잘린 사실은 로그에 남는다.
2. **기준가 거래소가 커버리지 필터에 걸려 빠졌다.** 4-a 의 `drop_short_coverage` 가
   업비트를 빼면 실전용 백테스트의 체결 가격이 사라진다. → `keep` 인자로 보호한다.
3. **`params_json` 에 타임프레임이 없었다.** 5분봉 결과와 1시간봉 결과가 DB 에서 구분되지
   않아 재현이 불가능했다. → `BacktestParams.timeframe` 추가.

#### 업비트 과거 데이터 한계 (실측 2026-09-03)

| 요청 | 결과 |
| --- | --- |
| 5분봉 7일 | 416봉 / 실제 4.2일 / 구멍 1곳 → 연속 216봉 |
| 1시간봉 30일 | 200봉 / 8.3일 / 구멍 없음 |
| 1시간봉 90일 | 560봉 / 56.6일 / 구멍 1곳 → 연속 360봉 |
| (비교) 바이낸스 5분봉 7일 | 2016봉 / 7.0일 / 구멍 없음 |

**실전용 백테스트는 1시간봉을 쓰는 것이 낫다.** 5분봉으로는 표본이 100여 봉밖에 안 남는다.

#### 실측 결과 (2026-09-03, 1시간봉 90일 요청)

| 심볼 | 기준 | 평가봉 | 거래 | 수익률 | 승률 | 손익비 | MDD |
| --- | --- | --- | --- | --- | --- | --- | --- |
| BTC | GLOBAL_CONSENSUS | 2059 | 98 | -9.42% | 48.0% | 0.91 | 12.42% |
| BTC | upbit | 259 | 14 | -5.96% | 35.7% | 0.70 | 9.41% |
| ETH | GLOBAL_CONSENSUS | 2059 | 117 | -8.91% | 44.4% | 1.09 | 17.67% |
| ETH | upbit | 259 | 16 | -3.93% | 37.5% | 1.09 | 7.56% |

**두 기준의 숫자를 비교하지 말 것.** 업비트 행은 데이터 한계로 구간이 훨씬 짧다
(259봉 vs 2059봉). "업비트가 덜 잃었다"가 아니라 **다른 기간을 잰 다른 숫자**다.
요구사항 4 가 "합치지 말라"고 한 이유가 이것이다.

**왜 지는지가 이번에 드러났다.** 1시간봉에서는 익절이 제대로 발동한다(BTC 32회/98거래).
승률 44~48%에 익절 +1.5% / 손절 -1.0% 면 총이익은 대략 `0.48×1.5 − 0.52×1.0 ≈ +0.20%/거래`인데,
왕복 비용이 신호검증용 약 0.18%, 업비트 실전용 약 0.20% 다. **비용이 기댓값을 통째로 먹는다.**
→ 재보정에서 익절·손절 폭을 비용 대비 충분히 크게 잡거나 진입 임계값을 올려 거래 수를
줄여야 한다. 이 계산은 [[traders-strategy-recalibration]] 과제의 출발점이다.

## 전략 재보정 1차 — 익절·손절 폭 조정 (2026-09-03)

사용자 지시로 (a) 익절·손절 폭 확대와 (b) 진입 임계값 상향을 함께 검토했다.
**신호를 (심볼×타임프레임)마다 한 번만 계산하고 매매 시뮬레이션만 반복**하는 구조로 바꿔
(`Backtester.compute_signals`) 96개 조합을 현실적인 시간에 돌렸다.

### 조합 탐색 결과 (1시간봉 90일, GLOBAL_CONSENSUS)

| 익절/손절 | BTC 임계80 | BTC 85 | BTC 90 | ETH 80 | ETH 85 | ETH 90 |
| --- | --- | --- | --- | --- | --- | --- |
| 1.5 / 1.0 (스펙) | -9.42% | -4.43% | +1.74% | -8.91% | -22.37% | -13.39% |
| 3.0 / 1.5 | +6.86% | +6.33% | +6.91% | -4.98% | -23.74% | -12.99% |
| 5.0 / 2.5 | +10.53% | +13.40% | +12.22% | +6.55% | -13.93% | -12.54% |
| 8.0 / 4.0 | +20.12% | +19.30% | +14.39% | +20.99% | -10.18% | -10.78% |

**(a) 익절·손절 확대는 지지된다 — 채택.** BTC 는 세 임계값 모두에서 넓힐수록 단조롭게
좋아졌다. 원인도 설명된다: 스펙의 1.5/1.0 은 손익비 **1.5:1** 이라 비용을 빼면 승률 40% 를
넘어야 본전인데, 실측 승률이 29~48% 였다. 2:1 로 넓히면 요구 승률이 33% 로 내려간다.
→ **기본값을 5.0 / 2.5 로 변경**했다. 8.0/4.0 이 더 좋았지만 **탐색 격자의 가장자리**라
채택하지 않았다(더 넓히면 더 좋아지는지 확인되지 않은 값이다).

**(b) 진입 임계값 상향은 지지되지 않는다 — 파라미터만 추가하고 기본값은 유지.**
거래 수는 확실히 줄지만(BTC 98→19건) 수익은 일관되게 좋아지지 않았다. ETH 는 임계값을
올릴수록 크게 나빠졌다. 더 중요한 이유는 **운영 등급과 어긋난다**는 것이다 — 운영은
Final 80 이상을 STRONG_BUY 로 내보내므로 백테스트만 85 를 요구하면 "운영은 사는데
백테스트는 안 사는" 신호가 생긴다. 실제로 올리려면 `STRONG_THRESHOLD` 를 함께 올리거나
**Step 5 에 매매 전용 임계값**을 둬야 한다. `BacktestParams.min_final_score` 로 언제든
탐색할 수 있게 열어 뒀다.

### 이 결과를 얼마나 믿을 수 있나

**90일 한 구간, 심볼 2개, 그리고 결과를 보고 값을 골랐다(in-sample).**
전체 96개 중 수익 조합은 22개(23%)뿐이고, 이웃한 칸의 부호가 뒤집히는 곳이 많다
(4h BTC 5.0/2.5 는 임계 80 에서 +10.09%, 85 에서 -20.57%). 그런 칸은 노이즈로 봐야 한다.

**방향(넓힐수록 낫다)은 비용·손익비라는 메커니즘으로 설명되므로 믿는다. 구체적인 숫자는
아직 out-of-sample 검증이 없다.** 다른 구간·다른 심볼로 재확인이 남아 있다.

## 타임프레임 확장 검토 (4h / 1d / 1w / 1M)

`timeframe_ms` 에 `w`(주) · `M`(월)을 추가했다. 월은 28~31일로 길이가 고정이 아니므로
연속성 판정에 **1.5배 허용치**(`GAP_TOLERANCE`)를 둬 정상 간격을 구멍으로 오인하지 않게 했다.

| TF | binance 확보 | 워밍업 100봉 소요 | 평가 가능 봉 | 판정 |
| --- | --- | --- | --- |
| 1h | 2160봉 | 4일 | 2059 | 충분 |
| 4h | 2400봉 (400일) | 17일 | 2300 | **충분** |
| 1d | 1500봉 (1499일) | 100일 | 1400 | **충분** |
| 1w | 473봉 (9년) | 700일 | 373 | 실측 거래 **0~1건** — 통계로 못 쓴다 |
| 1M | **110봉** (9년) | 3000일 | **10** | **불가능** — 데이터 자체가 없다 |

- **4h·1d 는 문제없고 유용하다.** 실제로 1d ETH 8.0/4.0 이 이번 탐색의 최고 성적이었다.
- **1w 는 돌아가지만 거래가 0~1건**이라 결론을 낼 수 없다. 워밍업 100봉이 2년을 먹는다.
- **1M 은 실질적으로 불가능하다.** BTC 월봉이 2017년 상장 이후 110개뿐이고 워밍업에
  100개를 쓰면 10봉이 남는다. 지표 창을 줄이면 되지만, 그러면 **운영과 다른 지표**를
  재게 되어 백테스트의 의미가 사라진다.
- 평가 봉이 200개 미만이면 경고를 남긴다(`MIN_MEANINGFUL_BARS`).
- **코인베이스는 4시간봉을 지원하지 않는다**(1h/2h/6h/1d만). 수집이 실패해도 해당 거래소만
  빼고 진행하므로 4h 백테스트는 4개 거래소로 돌아간다 — §3.2 최소 표본 3개는 지켜진다.

**다중 타임프레임을 운영 신호에 쓰는 것은 별개 문제다.** 지금 수집기는 5분봉 하나만 돌고
(`CANDLE_TIMEFRAME`), Step 7 의 적중률 horizon 도 5m/15m/1h 로 고정돼 있다. 백테스트에서
1d 가 좋다는 것과 그것을 운영에 태우는 것은 다른 작업이다.

## 관리자 계정 생성 — CLI (2026-09-03 결정)

**산출물**: `api/bin/make-admin.php`, `AuditRepository::ROLE_CHANGE`.
**지정된 관리자 이메일: `kysloving@gmail.com`**

```
php api/bin/make-admin.php <이메일>            승격
php api/bin/make-admin.php <이메일> --create   계정이 없으면 만들고 승격
php api/bin/make-admin.php --list              현재 관리자 목록
```

### 왜 CLI 인가

**승격을 HTTP 엔드포인트로 두지 않는다.** 그런 API 가 존재하는 것 자체가 권한 상승
취약점이다. 서버 셸에 접근할 수 있는 사람만 실행할 수 있어야 한다.
`.env` 에 관리자 이메일을 두는 방식(자동 승격)도 쓰지 않았다 — `.env` 가 곧 권한이 되어
관리 포인트가 늘어난다.

- **비밀번호를 명령줄 인자로 받지 않는다.** argv 는 `ps` 프로세스 목록과 셸 히스토리에
  그대로 남는다. 표준입력으로만 받고 `stty -echo` 로 화면에도 찍지 않는다.
- `api/bin` 은 웹 루트(`api/public`) 밖이라 URL 로 닿지 않는다. 그래도 배치가 바뀔 수 있어
  `PHP_SAPI !== 'cli'` 면 404 로 끝낸다.
- 승격·강등은 `audit_logs` 에 `ROLE_CHANGE` 로 남는다(마이그레이션 004).
  관리자 권한은 실거래 제어와 직결되므로 누가 언제 됐는지 기록이 없으면 안 된다.
- 이미 관리자면 아무 것도 하지 않고 알린다(멱등).

### 실측 검증 (2026-09-03, 로컬)

목록(비어 있음) → 미가입 이메일 승격 시도(안내) → 가입 후 승격 → 재실행(멱등) →
목록·감사 로그 확인 → 잘못된 이메일 거부 → `/bin/make-admin.php` 웹 접근 404.

**서버에는 아직 적용하지 않았다.** 대시보드 코드가 배포되지 않아 가입할 화면이 없다.
배포 후 `/app/` 에서 가입하고 서버에서 이 스크립트를 실행하면 된다.

## 참고 지표 추가 — VWAP (2026-09-03)

사용자 요청: **VWAP Divergence** 와 **볼린저밴드**를 참고/백테스트용 지표로 추가.

### 볼린저밴드는 이미 있었다

BB(20, 2) 밴드 3종 · 위치 분류(`BELOW_LOWER`/`LOWER_HALF`/`UPPER_HALF`/`ABOVE_UPPER`) ·
배점 규칙(하단 이탈 후 복귀 +10 / 상단 돌파 지속 -10) · `ai_signals.bollinger_position`
컬럼까지 Step 1 부터 구현돼 있다. **추가 작업 없음.**

### VWAP — 추가하되 배점에는 넣지 않는다

`calculator.compute_session_vwap()` + `vwap_divergence_pct()`.
**세션 앵커드(UTC 자정 리셋)** — 트레이딩뷰가 보여주는 것과 같은 정의다.

**롤링 VWAP 으로 먼저 구현했다가 실측 후 바꿨다.** 20봉 롤링 VWAP 은 사실상 이동평균이라
"가격이 평균에서 얼마나 떨어졌나"를 RSI 와 똑같이 재고 있었다:

| 정의 | RSI 와의 r | 판정 |
| --- | ---: | --- |
| 롤링 VWAP(20봉) 괴리율 | **+0.895** | 중복 — 넣을 이유 없음 |
| **세션 앵커드 괴리율** | **+0.799** | 강한 상관 — 참고용으로만 |

세션 앵커드도 RSI 와 r=0.799 라 **배점표에 넣지 않는다.** 화면 표시와 백테스트 참고용이다.
점수에 넣으려면 배점표 재설계(Step 16)에서 중복을 정리한 뒤에 해야 한다.

**한계:** 100봉 창으로 지표를 돌리므로 창을 벗어난 세션 앵커는 볼 수 없다. 5분봉에서
UTC 하루는 288봉이라 창에 담긴 부분만 계산된다 — 트레이딩뷰 값과 정확히 일치하지 않는다.

**상용 스크립트를 옮기지 않았다.** "VWAP Divergence" 라는 이름의 인디케이터가 여럿 있으나
계산이 제각각이고 비공개인 것도 많다. 구현한 것은 **공개된 표준 정의**(가격과 VWAP 의 괴리율)다.
비공개 Pine Script 는 재현할 수도 없고 해서도 안 된다(약관·저작권).

### 새 지표를 넣을 때의 절차 (앞으로 고정)

1. 계산을 `calculator.py` 에 구현한다 — **관리자가 화면에서 수식을 입력하게 만들지 않는다.**
   입력한 코드를 서버에서 실행하는 구조는 관리자 계정이 뚫리면 서버가 통째로 뚫린다.
2. **기존 지표와의 상관계수를 잰다.** `|r| >= 0.8` 이면 중복이므로 넣지 않는다.
   `tests/test_indicator_independence.py` 에 계산기와 기준을 뒀다.
3. `|r| < 0.6` 이 아니면 **참고용으로만** 쓰고 배점에는 넣지 않는다.
4. 배점에 넣으려면 백테스트를 전면 재검증하고 `params_json` 에 지표 세트를 기록한다 —
   지표 구성이 바뀌면 과거 신호와 지금 신호를 비교할 수 없게 된다.

**Step 18** 은 이 위에 관리자 토글 UI 와 등급별 노출(§17-1 "지표 3종/5종/8종")을 얹는다.

## 전략 자문 검토 (2026-09-03) — 실측으로 검증한 것

외부 전략 자문을 받아 **주장을 그대로 받지 않고 이 프로젝트 데이터로 검증**했다.
확인된 것은 로드맵에 넣고, 틀린 것은 이유를 남긴다.

### ✅ 검증됨 — 지표가 서로 중복이다 (가장 큰 발견)

BTC 1시간봉 620개 표본, 피어슨 상관계수:

| 조합 | r | 판정 |
| --- | ---: | --- |
| 스토캐스틱 %K ↔ CCI | **+0.844** | 사실상 중복 |
| RSI ↔ CCI | **+0.822** | 사실상 중복 |
| RSI ↔ 스토캐스틱 %K | +0.763 | 강한 상관 |
| MACD 히스토그램 ↔ 스토캐스틱 | +0.638 | 강한 상관 |
| **ADX ↔ 나머지** | +0.07 ~ +0.50 | **독립적** |

**현재 배점표는 사실상 같은 신호에 최대 40점을 준다** (RSI 20 + 스토캐스틱 ±10 + CCI +10).
"여러 근거가 일치한다"는 착각이 점수로 굳어진다. ADX 만 진짜로 독립적이다.
→ **Step 16**. 중복 지표를 하나의 모멘텀 축으로 묶고, 남는 배점을 독립 축(추세·거래량·파생)에 준다.

### ✅ 검증됨 — 파생 데이터를 지금 바로 가져올 수 있다

| 거래소 | 펀딩비 | 미결제약정 | OI 이력 | 청산 |
| --- | --- | --- | --- |
| binance | O | O | O | X |
| bybit | O | O | O | X |
| okx | O | O | O | X |

실측 펀딩비도 정상 수신(BTC 6.5e-05 등). **ccxt 로 이미 붙어 있어 추가 의존성이 없다.**
청산 데이터만 ccxt 통합 API 가 없어 거래소별 직접 호출이 필요하다.
→ **Step 14**. 자문의 지적대로 **가격 상승 + OI 증가(신규 롱)** 와 **가격 상승 + OI 감소(숏 커버)**
는 현물 지표로 절대 구분되지 않는다. 지표를 더 얹는 것보다 이쪽이 정보량이 크다.

### ⚠️ 부분적으로 틀림 — "메이저는 합의율이 늘 95~100%"

BTC·ETH 1시간봉 각 100 표본 실측:

| 심볼 | 평균 | 중앙값 | 최소 | 95% 이상 비율 | 분포 |
| --- | ---: | ---: | ---: | ---: | --- |
| BTC | 90.8% | 100% | 40% | **61%** | 40%×1 / 60%×5 / 80%×33 / 100%×61 |
| ETH | 92.8% | 100% | 60% | **68%** | 60%×4 / 80%×28 / 100%×68 |

**늘 95~100% 가 아니다.** 자문은 합의를 *가격 일치*로 가정했는데, 우리 구현은
**거래소별 지표 방향 일치**를 잰다. 거래소마다 캔들이 조금씩 달라 지표 방향이 갈린다.

다만 **분포가 100% 에 크게 쏠려 있는 것은 사실**이라(61~68%), 정보량이 낮다는 지적의
방향은 맞다. → 합의율을 점수 구성요소로 20% 주는 것이 적절한지 Step 16 에서 재검토한다.
"신호 생성 근거"보다 **"이 신호를 믿어도 되는지 판정하는 필터"** 로 쓰는 쪽이 낫다는 제안은 타당하다.

### ✅ 검증됨 — 김치 프리미엄은 환율 소스가 없으면 불가능하다

업비트 BTC/KRW 108,650,000 · 바이낸스 BTC/USDT 79,089.75 → 암시 환율 1,373.8.
**환율을 모르면 "프리미엄 변화"와 "환율 변동"이 섞여 값이 무의미해진다.**
→ **Step 15** 는 환율 소스 확보가 선행이다(한국은행 API·공개 FX API 등).

우리는 이미 `entry_price_global`(USD)과 `entry_price_upbit`(KRW)를 **둘 다 저장하고 있는데
그 관계를 쓰지 않는다.** 자문의 지적대로 이것이 해외 서비스가 못 하는 지점이다.

### 받아들인 것 — 검증 없이도 논리가 분명한 지적

1. **5분 ±0.2% 는 비용을 빼면 동전 던지기.** 우리 백테스트가 이미 같은 결론을 냈다
   (왕복 비용 0.18~0.20% vs 기댓값 +0.20%). → [[traders-strategy-recalibration]] 과 같은 묶음.
2. **Step 7 의 적중률 정의(±0.2%)가 백테스트(삼중 배리어)와 다르다.** 두 개가 서로 다른 것을
   재고 있다. 백테스트는 이미 익절·손절·시간제한으로 라벨링하는데 적중률만 ±0.2% 다.
   → **Step 7 착수 전에 정의를 백테스트에 맞춘다.**
3. **워크포워드 검증 필수.** 앞선 조합 탐색(96개)은 전부 in-sample 이었고 그 한계를 이미
   기록해 뒀다. → **Step 17**.
4. **국면(regime) 분리.** 같은 RSI 30 이 추세장과 횡보장에서 정반대 의미다.
   ADX 가 독립적이라는 실측 결과가 국면 판정 축으로 쓸 근거가 된다. → Step 16.
5. **신호를 줄이고 전부 공개.** `ai_signal_results` 가 이미 모든 신호 결과를 기록한다.
   "틀린 것 포함 전부 공개"는 기술이 아니라 정직함이 만드는 차별점이고,
   **법률 검토의 "적중률을 마케팅 헤드라인으로 쓰지 말 것"과도 정확히 맞는다.**
   해시 체인 봉인으로 사후 수정 불가능하게 만들면 더 강해진다. → Step 17.

### ✅ 적용 완료 — AI 를 점수에서 뺐다 (2026-09-03 사용자 결정)

**AI 는 이제 설명 전용이다.** `prompt.md` §3.3 가중식을 개정했다.

    이전:  Tech 45% + Consensus 20% + AI 20% + Risk 15%
    현재:  Tech 56.25% + Consensus 25% + Risk 18.75%   (AI 몫을 빼고 비례 재분배)

- `final_score()` 가 **AI 점수를 인자로 받지 않는다** — 받을 수 있게 열어두면 언젠가 누가 넣는다.
- `with_ai()` 는 `ai_score` 를 **기록만** 하고 등급·점수를 바꾸지 않는다.
  기록을 남기는 이유는 나중에 "룰과 AI 중 무엇이 맞았나"를 데이터로 비교하기 위해서다.
- **확률 합계 이상 시 HOLD 강등을 없앴다.** AI 가 점수에서 빠진 뒤로는, 룰이 낸 신호를
  LLM 이 JSON 을 잘못 만들었다는 이유로 약화시키는 것이 앞뒤가 안 맞는다.
  대신 `data_sources_json.ai.probabilities_reliable=false` 로 표시하고 **화면이 그 확률을
  숨긴다** — 신뢰할 수 없는 숫자를 띄우는 것이 더 나쁘다. (Step 2 DoD 에서 벗어나는 부분)
- 화면의 점수 구성에서 AI 막대를 빼고, "점수는 규칙과 통계로만 산출하며 AI 는 근거·위험
  요소를 설명하는 데만 사용합니다" 를 명시했다.

## 법률 검토 반영 (2026-09-03)

외부 법률 검토를 받았다. 전문은 **`docs/LEGAL.md`**, 상시 참고용 요약은 메모리
`traders-legal-constraints`. **모든 UI·카피 작업에 적용된다.**

### 결론

🟠 유료 BUY/SELL 신호 부분 때문에 자본시장법상 유사투자자문업·투자자문 규제를 반드시
검토해야 하는 모델. 다만 🟢 **"투자자문 판매"가 아니라 "분석 도구 구독"** 으로 구조를
명확히 하면 현실적인 사업 모델. 핵심은 **우리 서비스가 돈이나 코인을 만지지 않는 것.**

**지금 구조에서 잘하고 있다고 확인된 것**(유지할 것): 고객 돈 안 받음 / 고객 계좌 운용 안 함 /
**고객 API Key 안 받음** / 본인 자동매매는 내부 기능 / 분석 근거 공개 / 백테스트와 실전 구분.
→ **고객 API Key 를 받는 구조로 발전시키지 말 것.**

### 화면에 즉시 반영한 것 (배포 완료)

1. **신호 등급에서 "매수/매도" 제거.** 자문의 🔴 목록에 "매수 추천/매도 추천"이 있다.
   유료 + 특정 코인 + 매수/매도 가 합쳐지면 투자 추천 서비스로 평가될 소지가 커진다.
   → `적극 매수/매수/관망/매도/적극 매도` → **`상승 신호 강함/상승 신호/중립/하락 신호/하락 신호 강함`**
   DB 의 `signal_type` ENUM 은 내부 값이라 그대로 둔다(스키마·API 계약 불변).
2. **확률에 "모델 추정" 라벨과 고지 추가.** `상승 가능성 81%` 는 "AI가 81% 확률로 오른다고
   보장한다"로 읽힌다. → `모델 추정 확률 (상승/보합/하락)` + **"과거 데이터 기반 모델
   출력값이며 실제 미래 수익률을 보장하지 않습니다."**
3. **백테스트에 시뮬레이션 고지와 비용 투명성 추가.** 실현되지 않은 수익률 제시는
   자본시장법상 광고 금지 대상이다. → 상단 고지("과거 데이터로 전략을 재생한 시뮬레이션
   결과… 과거 성과는 미래의 수익을 보장하지 않습니다"), 열 이름 `시뮬레이션 수익률`,
   **비용 반영 열**(수수료·슬리피지 실제 값), 데이터 출처 문구.

### 앞으로 지킬 것

- **"AI 가 판단했으므로 자문이 아니다" 는 면책이 되지 않는다.** 그 논리에 기대는 카피 금지.
- **명칭만 바꾸고 실질이 투자자문이면 안전하지 않다.** 기능과 전달 의미를 함께 본다.
- 적중률은 **공개하되 마케팅 헤드라인으로 쓰지 않는다.** 같은 화면에 표본 수·기간·비용
  반영 여부를 함께 표시한다(앞서 정한 "전체 통계 공개" 결정은 이 조건 아래 유지).

### 출시 전 미완 (Step 13 결제의 선행 조건)

1. 변호사·금융당국에 **유사투자자문업 신고 여부** 확인. "신고 = 자유롭게 자문 가능"이 아니다.
2. **거래소별 API 약관** — Commercial Use / Redistribution / Caching / Derived Data.
   "가져올 수 있다"와 "상업 SaaS 에서 재배포할 수 있다"는 다른 문제다.
3. **TradingView 약관** — Webhook 데이터를 상업 서비스에서 재가공·저장·표시 가능한지.

## 로컬 확인 환경 (2026-09-03)

`./api/bin/serve-local.sh` → **http://127.0.0.1:8080/app/**

저장소 `.env` 의 `DB_HOST=mysql` / `REDIS_HOST=redis` 는 **도커 서비스명**이라 호스트에서
PHP 를 직접 띄우면 해석되지 않는다. `.env` 를 고치면 docker-compose 배포가 깨지므로,
스크립트가 그 두 개만 `127.0.0.1` 로 덮어써서 실행한다.

**로컬 `.env` 의 `JWT_SECRET` 을 실제 값으로 교체했다.** 자리표시자면 `Jwt::secret()` 이
거부해서 앱이 아예 뜨지 않는다. **서버 `.env` 는 아직 자리표시자다 — 배포 전에 교체해야 한다.**

### 등급 판정이 세 곳으로 갈라져 있던 버그 (실사용에서 발견)

관리자로 로그인했는데 **등급 배지가 FREE** 로 나왔다. `Guard::plan()` 은 관리자를 PRO 로
보게 고쳤지만, **로그인 응답과 관리자 유저 목록은 각자 등급을 계산하고 있었다.**
증상이 경로마다 달랐다 — 로그인 직후엔 FREE, 새로고침하면 PRO, 유저 목록에선 다시 FREE.

→ `UserRepository::planForUser(array $user)` 하나로 모으고 세 곳이 모두 그것을 쓴다.
**같은 판정을 두 번 구현하면 반드시 어긋난다.**

### 사용자가 겪은 "메뉴 클릭 시 로그아웃"

원인은 검증 후 정리하면서 `users` 테이블을 비운 것이다. 남아 있던 JWT 의 `sub` 가 가리키는
유저가 사라져 모든 API 가 401 을 냈고, `app.js` 가 401 에서 자동 로그아웃한다(의도된 동작).
**정리 작업이 사용자 세션을 끊었다** — 데모 데이터를 지울 때는 알려야 한다.

## Step 9-b — 관리자 화면 · 점수 추이 차트 — **완료 2026-09-03**

**산출물**: `Http/Admin.php`, `Repository/AdminRepository.php`, `assets/app.js` 의
관리자 화면과 `sparkline()`, `RedisStore.load_ai_mode/save_ai_mode`,
`AiBudget.current_mode()`.

```
GET   /api/v1/admin/users        PATCH /api/v1/admin/users/{id}   (등급 수동 부여)
GET   /api/v1/admin/ai-budget    PATCH /api/v1/admin/ai-budget    (AI 예산 전환)
GET   /api/v1/admin/system       (엔진 생존·운영 지표)
```

### 정한 것

1. **AI 예산 모드를 런타임에 바꿀 수 있게 했다.** 기존에는 `.env` 의 `AI_MODE` 를 기동 시
   한 번 읽어 고정이라, 끄려면 프로세스를 재시작해야 했다 — **비용이 새는 상황에서
   재시작을 기다릴 수 없다.** Redis `settings:ai_mode` 가 있으면 그것이 이기고, 없으면
   `.env` 가 기본값이다. 조회 실패 시에는 기본값으로 물러선다.
   실측: `.env=full` 인 엔진에서 화면으로 `off` 를 누르자 `allow()` 가 즉시 False 가 됐다.
2. **역할(관리자) 변경은 화면에 두지 않았다.** 승격 API 가 존재하는 것 자체가 권한 상승
   취약점이라 CLI(`api/bin/make-admin.php`)로만 한다. 화면에는 그 이유를 적어 뒀다.
3. **관리자는 항상 PRO 로 본다.** 운영자가 합의율·근거·이력을 못 보면 유저 문의에 답하거나
   신호가 이상한지 판단할 수 없다. 관리자는 CLI 로만 만들어지므로 이 우회로 결제를
   건너뛸 방법은 없다.
4. **관리자가 아니면 403 이 아니라 404.** 자동매매는 고객에게 제공하는 기능이 아니므로
   (README §17) 그 경로의 존재 자체가 새어나가면 안 된다.
5. **LIVE 전환에 확인 대화상자를 뒀다.** 실제 돈이 움직이기 시작하는 지점이다.
6. **차트는 인라인 SVG.** 차트 하나에 번들러와 의존성을 들이지 않는다(9-a 와 같은 이유).
   진입 기준선(80)을 점선으로 함께 그려 "이 위로 올라가야 매수"가 보이게 했다.
7. **등급 부여는 구독 행을 덮어쓰지 않고 새로 만든다.** 기존 활성 구독은 해지 처리한다 —
   "언제 어떤 등급이었나"가 남아야 결제(Step 13)를 붙일 때 대조할 수 있다.

### 렌더링 검증에서 잡은 것 1건

**엔진 상태 배지에 시장 방향 색(`--up`/`--down`)을 재사용하고 있었다.** 그 토큰은 로케일에
따라 뒤집히므로, `en` 으로 바꾸면 **"정상"과 "중단"의 색이 서로 바뀐다.** 상태는 로케일과
무관하므로 `.status.ok/.bad` 로 고정색을 따로 뒀다.

### 실측 검증

일반 유저의 `/admin/*` 접근 404, 관리자 접근 정상. AI 예산 off 전환 → Redis 키 확인 →
엔진이 즉시 반영. 등급 수동 부여(free→pro) 후 목록 반영. 감사 로그에
`AI_BUDGET_CHANGE`·`PLAN_GRANT`·`ROLE_CHANGE` 3건 기록. 브라우저 렌더링 콘솔 오류 0건.
검증 데이터는 모두 삭제했다.

### 남은 것

매매 내역 위젯(보유 코인·손익·잔고)은 **Step 5 이후**다 — 체결 내역 테이블이 아직 없다.
랜딩/요금제 페이지, `/market/summary`·`/backtest/logs` 등급 차등도 남아 있다.

## Step 9-a — 대시보드 — **완료 2026-09-03**

**산출물**: `api/public/app/index.html`(SPA 셸), `assets/app.css`(디자인 시스템),
`assets/app.js`(API 클라이언트 + 화면 4종). 접속 경로는 **`/app/`**.

화면: 로그인·회원가입 / 대시보드(마켓 카드 + 강한 신호) / 신호 목록 / 신호 상세(점수 구성
막대·합의율·근거·위험) / 백테스트 결과(기준별 탭).

### 정한 것

1. **빌드 도구를 쓰지 않는다.** 이 저장소에 node 툴체인이 없고, 화면 몇 개에 번들러를
   들이면 배포(rsync)와 CI 가 통째로 복잡해진다. 순수 HTML/CSS/JS 에 해시 라우팅.
2. **DB 에 직접 붙지 않는다** (Step 9 DoD). Step 6 REST API 만 호출한다.
3. **상승/하락 색을 `--up`/`--down` 토큰으로만 쓴다.** 한국·일본은 빨강=상승,
   서구는 초록=상승이라 `data-locale` 로 뒤집는다. 값을 하드코딩하면 절반의 유저가
   신호를 정반대로 읽는다. **색과 함께 ▲▼ 기호를 항상 붙인다** — 색각 이상 대응.
4. **순흑(#000)을 쓰지 않는다.** 대비가 과해 숫자를 오래 보기 피로하고 경계선이 표현되지
   않는다. 짙은 남색(#0b1020) 바닥에 고대비 텍스트.
5. **잠긴 항목은 숨기지 않고 자물쇠로 표시**한다. 무엇을 못 보는지 알아야 업그레이드
   유인이 생긴다. 차단 자체는 서버가 하므로 화면은 "없는 필드"를 그릴 뿐이다.
6. **법적 고지를 전 화면 하단에 노출**한다 (prompt.md §7-3 문구 그대로, 로그인 화면 포함).

### 렌더링 검증에서 잡은 것 2건

브라우저(headless chromium)로 전 화면을 실제로 띄워 확인했다. 콘솔 오류 0건.

- **"평가 전"이 자물쇠(🔒)로 그려지고 있었다.** 등급 제한과 데이터 없음이 같은 기호가 되어
  "돈 내면 보이나?"로 오해된다. `.pending`(자물쇠 없는 점선)을 따로 뒀다.
- **FREE 가 볼 수 있는 창이 15~60분뿐이었다.** `historyDays=0` 을 "지연 창 언저리만"으로
  구현했더니 실측에서 신호가 **하나만** 보였다. §17-1 의 "히스토리 없음"은 **적중률 이력**을
  뜻하지 신호 목록을 막으라는 것이 아니다. FREE 를 **1일**로 열었다
  (지연 15분은 그대로 — 3분 전 신호는 여전히 안 보인다).

### 남은 것 (Step 9-b)

관리자 화면(유저 관리 · AI 예산 스위치 · 안전장치 제어), 점수 추이 차트, 랜딩/요금제 페이지.
매매 내역 위젯은 Step 5 이후. `/market/summary`·`/backtest/logs` 의 등급 차등도 아직 없다.

## Step 6-b — 인증 · 등급 · Rate Limit — **완료 2026-09-03**

**산출물**: `Http/Auth.php`(가입·로그인·로그아웃·me), `Auth/Plan.php`(등급 판정),
`Auth/Guard.php` 확장(폐기 확인·등급 게이트), `Utils/RateLimiter.php`,
`Repository/{User,Audit}Repository.php`, **마이그레이션 004(감사 로그)**.
테스트 36건(PHP). 마이그레이션은 로컬·서버 모두 적용 완료.

```
POST /api/v1/auth/register   POST /api/v1/auth/login
POST /api/v1/auth/logout     GET  /api/v1/auth/me
```

### 정한 것

1. **로그아웃은 Redis 폐기 목록(denylist)으로 구현한다.** JWT 는 무상태라 발급한 토큰을
   되돌릴 방법이 없는데, **실거래 권한이 걸린 관리자 토큰**을 "클라이언트에서 지웠으니
   끝"으로 둘 수 없다. `jti` 클레임을 추가하고 로그아웃 시 남은 수명만큼 Redis 에 올린다.
   **폐기 확인이 실패하면(Redis 장애) 통과가 아니라 차단**이다 — 폐기된 토큰을 통과시키는
   쪽이 훨씬 위험하다. (Rate Limit 과 반대 방향의 판단이다.)
2. **등급 차등은 SQL 에서 건다.** 응답에서 지우는 방식이면 쿼리는 이미 유료 데이터를 읽은
   뒤라 실수 한 번에 새어 나간다. FREE 지연(15분)과 이력 기간은 `WHERE created_at` 조건이고,
   숨김 필드는 응답에서 **키 자체를 삭제**한다(null 이면 "값이 없다"로 읽히지만, 없으면
   "이 등급에는 제공되지 않는다"가 되어 화면이 잠금 표시를 띄울 근거가 된다).
3. **PRO 게이트는 API 에 있다.** 백테스트 실행과 웹훅 발급에 `requirePlan(PRO)`.
   화면에서 버튼만 숨기면 API 직접 호출로 우회된다. 등급 부족은 401 이 아니라 **402**
   (`PLAN_REQUIRED`) — 화면이 "로그인하세요" 대신 "업그레이드"를 띄워야 한다.
4. **로그인은 없는 계정에도 해시 검증을 돌린다.** 안 그러면 응답 시간 차이로 가입 여부를
   알아낼 수 있다. 가입은 중복 이메일에 409 를 주는데, 이는 존재를 노출하지만 알려주지
   않으면 유저가 진행할 수 없어 감수한다.
5. **감사 로그 실패가 본 동작을 막지 않는다.** 기록 실패는 error_log 로만 남기고 진행한다 —
   로그 저장소 장애가 로그인 전면 중단이 되면 안 된다. `user_id` 는 ON DELETE SET NULL
   이라 계정을 지워도 "누가 LIVE 를 켰었나"는 남는다.

### 테스트가 잡은 버그 1건

`Plan::historyDays()` 가 `?? 0` 을 쓰고 있었다. PRO 의 값이 `null`(무제한)인데 널 병합이
**0(이력 없음)으로 뒤집어**, PRO 가 이력을 전혀 못 보게 되는 상태였다. 무제한과 없음은
정반대인데 한 글자 차이로 갈린다. `array_key_exists` 로 고쳤다.

### 실측 검증 (2026-09-03)

가입(중복 409·짧은 비밀번호 400) → 로그인(틀린 비밀번호·없는 계정 모두 401) → `/auth/me`,
**로그아웃 후 같은 토큰이 401 로 거부**되고 Redis 폐기 키가 TTL 3600 으로 남는 것 확인.
FREE 로 조회하면 2분 전 신호는 안 보이고 40분 전 것만 보이며 합의율·위험·점수가 감춰지고
근거가 1줄로 잘린다. PRO 구독을 넣으면 둘 다 보이고 전체가 열린다.
백테스트 실행·웹훅 발급이 FREE 402 → PRO 202/201 로 바뀌는 것, 인증 엔드포인트가
분당 10회에서 429 로 막히는 것까지 확인했다. 검증 데이터는 모두 삭제했다.

### 남은 것

`/market/summary` 와 `/backtest/logs` 에는 아직 등급 차등이 없다(전 등급 동일).
§17-1 의 "지표 3종/5종/8종"도 신호 응답에만 적용돼 있다. Step 9 에서 화면을 붙이며
필요한 만큼 좁힌다.

## Step 6-a — REST 엔드포인트 5종 — **완료 2026-09-03**

**산출물**: `Auth/Guard.php`(공용 인증 가드), `Repository/{User,Signal,Safety}Repository.php`,
`Utils/RedisClient.php`, `Http/{Signals,Market,Backtest,Safety}.php`, 라우터 접두사 테이블,
그리고 Python 쪽 `backtest/worker.py`.

```
GET   /api/v1/signals/latest?symbol=&limit=&before_id=
GET   /api/v1/signals/strong
GET   /api/v1/signals/{id}
GET   /api/v1/market/summary
POST  /api/v1/backtest/run      → 202 (큐 적재)
GET   /api/v1/backtest/logs?reference_exchange=
GET   /api/v1/safety/state
PATCH /api/v1/safety/state
```

### 정한 것

1. **`POST /backtest/run` 은 비동기다.** 실측으로 한 번에 16초~수 분이 걸린다. HTTP 요청을
   붙잡으면 타임아웃이 나고 재시도가 같은 백테스트를 중복 실행한다. → PHP 는 Redis 리스트
   `backtest:queue` 에 넣고 **202** 를 돌려주고, Python 워커가 꺼내 돌린다.
   재생은 CPU 작업이라 워커 안에서 `asyncio.to_thread` 로 옮겼다 — 안 옮기면 백테스트가
   도는 몇 분 동안 거래소 수집이 전부 지연된다.
2. **`/backtest/logs` 는 `reference_exchange` 가 필수 인자다.** 빼먹으면 400 이다.
   신호검증용과 업비트 실전용을 한 목록에 섞으면 화면에서 Step 4 요구사항 4 를 위반한다.
3. **페이지네이션은 OFFSET 이 아니라 id 커서다.** 신호는 계속 쌓이므로 페이지를 넘기는
   사이에 새 행이 들어오면 OFFSET 은 같은 행을 두 번 보여주거나 건너뛴다.
4. **`/market/summary` 는 DB 가 아니라 엔진의 Redis 캐시를 읽는다.** TTL 이 지나 키가 없으면
   `stale: true` 로 내려준다 — 낡은 값을 현재가처럼 보여주느니 엔진이 멈췄다는 사실이
   화면에 드러나는 편이 낫다.
5. **`PATCH /safety/state` 는 모르는 필드를 무시하지 않고 400 을 낸다.** 조용히 무시하면
   "바꿨다고 생각했는데 안 바뀐" 상태가 되는데, 실거래 설정에서 그건 위험하다.
6. **인증 로직을 `Auth/Guard` 로 통합**했다. Step 3-a 의 `Webhooks` 안에 있던 것을 꺼내
   재사용한다 — 엔드포인트가 다섯 개 늘어난 상태에서 복사해 두면 인증 규칙이 여섯 군데로 갈라진다.
   관리자 판정(`Guard::admin()`)도 함께 넣었다(Step 9-b 관리자 화면이 쓴다).

### 실측 검증 (2026-09-03)

PHP API + Python 엔진을 함께 띄워 전 경로 확인 — 인증 없이 401, 신호 목록·커서 페이지네이션,
강한 신호 필터, `market/summary` 의 stale 판정(엔진 정지 시 true / 가동 시 4개 거래소 가격),
`PATCH /safety/state` 의 PAPER→LIVE 전환과 잘못된 값 400.
**백테스트는 `POST /run` → 큐 적재 → 워커 소비 → `backtest_logs` 저장 → `GET /logs` 조회**까지
한 줄로 확인했다(BTC 1h 30일, 17초, 거래 15건). 검증 데이터는 모두 삭제했다.

### 남은 것 (Step 6-b)

**현재 모든 엔드포인트가 인증을 요구하지만 로그인 엔드포인트가 없다.** 토큰을 발급할 방법이
개발용 `Jwt::issue()` 뿐이라, 지금 상태로는 실사용이 불가능하다. 6-b 에서 로그인/로그아웃과
Rate Limit, 그리고 감사 로그 마이그레이션(004)을 붙인다.

## 제품 정의 결정 (2026-09-03)

### 1. FREE 등급의 실제 상품 — 지연 + 근거 감춤

README §17-1 의 "거래소 커버리지: 업비트 단독"은 **구현 불가능하다.** 우리 신호는 다중 거래소
합의로 만들어지고, 업비트 하나로는 §3.2(유효 거래소 3개 미만)에 걸려 **항상 HOLD** 다.
즉 FREE 에게 줄 신호가 존재하지 않는다.

→ **FREE 는 같은 합의 신호를 15~30분 지연해서 받고, 근거(합의율·지표·AI 설명)는 감춘다.**
"실시간성"과 "왜 그런가"가 유료 가치가 된다. §17-1 의 "거래소 커버리지" 행은 재정의가 필요하다.

### 2. 결제·PG 연동 — 로드맵 최종 단계로

`prompt.md:57` 에 "회원가입/인증, PG 결제 연동"이 있으나 Step 1~12 에 해당 스텝이 없었다.
README:551 의 출시 순서(무료 베타 → 성능 검증 → 법적 확인 → 월 구독)에 따라
**결제는 맨 마지막 스텝으로 미룬다. 회원가입은 Step 6-b 에서 지금 만든다.**
그때까지 등급은 관리자가 수동 부여한다.

### 3. 유료 BUY/SELL 신호 — 개발은 하되 유료화만 보류

README §18 에서 🟠 로 표시된 항목이다. **개발을 멈추라는 뜻이 아니다** — 무료 베타로
운영하면서 적중률을 쌓고, **과금 시작 전에** 유사투자자문 해당 여부를 확인한다
(README:551 의 순서 그대로). 사용자가 별도로 알아본 뒤 알려주기로 했다.

### 4. 적중률 — 전체 통계는 공개, 개별 이력은 유료

§17-1 은 FREE 에게 "히스토리/적중률 없음"이지만, 적중률은 이 서비스를 믿게 만드는 유일한
증거이고 무료 베타의 목적이 그 검증이다. 감추면 가입 유인이 사라진다.
→ **전체 통계(적중률 %, 표본 수)는 비로그인도 보이고, 개별 신호 이력은 등급별 제한.**

### 5. 심볼 수는 비용 문제이지 데이터 문제가 아니다 (실측 2026-09-03)

| 조건 | 종목 수 |
| --- | --- |
| 5개 거래소 전부 상장 | **93종** |
| 4개 이상 | 183종 |
| 3개 이상 (합의 성립 최소) | 307종 |

**데이터는 넉넉하다.** BTC/ETH/XRP/SOL/DOGE 5종은 시작 범위일 뿐이고, 합의를 낼 수 있는
종목이 93개나 된다(ADA·AVAX·DOT·LINK·ATOM·ARB 등).

심볼을 늘릴 때 실제로 늘어나는 비용:
- **시세 수집은 무료다** (거래소 API 키도 불필요 — Step 1 실측)
- **AI 호출이 선형으로 는다**: seed 모드 심볼당 약 $0.3/월, full 모드 약 $4/월
- **CPU 가 선형으로 는다**: 지표 계산이 (거래소 × 심볼) 조합마다 돈다. 지금 25 조합.
  93종이면 465 조합이라 스로틀 재조정이 필요하다.
- 거래소별 WebSocket 구독 수 상한

**PRO·관리자의 추가 알트코인 조회는 이미 가능한 코드가 있다.** 백테스트가 정확히 그 일을
한다 — 임의 심볼의 과거 캔들을 REST 로 긁어 운영과 같은 경로로 지표·합의·점수를 낸다.
"상시 수집 목록에 넣기"(비용 선형 증가)와 "요청 시 즉석 분석"(요청당 비용)을 분리하면,
**PRO 에게 온디맨드 조회를 열어주는 것이 상시 수집을 늘리는 것보다 훨씬 싸다.**
→ Step 9 이후 기능 후보로 남긴다.

### 6. 브랜드·디자인 톤

- **브랜드명: Traders** (확정)
- **톤: 다크 계열 + 전문가 느낌, 단 시인성 우선.** 순흑(#000)이 아니라 짙은 남색 계열
  바닥에 고대비 텍스트. 강조색은 절제하고 데이터에만 쓴다.
- **상승/하락 색이 로케일에 따라 뒤집힌다.** 한국·일본은 **빨강=상승 / 파랑=하락**,
  서구는 **초록=상승 / 빨강=하락**이다. Step 10 이 ko/en/ja 를 지원하므로
  **색 의미를 로케일에 묶어야 한다** — 하나로 고정하면 절반의 유저가 반대로 읽는다.
  빨강·초록 조합은 색각 이상에서 구분이 어려우므로 **색에만 의존하지 말고
  기호(▲▼)와 수치를 함께** 표시한다.

## Step 9 대시보드 요구사항 (2026-09-03 사용자 추가 지시)

### 요구사항

1. **관리자 — 자동매매 관리·설정·내역**: 투자 중인 코인, 수익, 손절/익절, 최종 잔고
2. **관리자 — 사이트 운영 메뉴**: 유저 관리(유/무료 구분), **AI 토큰 사용량 제어 스위치**
3. **유저 — 등급별 차등 UI**: README §17-1 (FREE / BASIC / PRO)

| 항목 | FREE | BASIC | PRO |
| --- | --- | --- | --- |
| 거래소 커버리지 | 업비트 단독 | 3개 합의 | 5개 전체 |
| 신호 지연 | 15~30분 | 실시간 | 실시간 |
| 지표 | 기본 3종 | +거래량/호가 | 전체 8종 |
| AI 설명 | 요약 1줄 | 근거 요약 | 근거+위험요소 전문 |
| 히스토리/적중률 | 없음 | 7일 | 전체 |
| 백테스트 | 결과 열람만 | 결과 열람만 | 직접 실행 |
| TradingView 웹훅 | ✕ | ✕ | ✓ |

### 이 요구사항이 드러낸 구멍 4개 (착수 전 해결 필요)

**1. 체결 내역 테이블이 없다.** "투자 중인 코인 / 수익 / 손절·익절 / 최종 잔고"를 보여주려면
실제 주문·체결 기록이 있어야 하는데, `init.sql` 6개 테이블 어디에도 없다
(`trading_safety_state` 는 설정이지 내역이 아니다). **Step 5 가 만들고 Step 9-b 가 읽는다** —
즉 관리자 매매 내역 화면은 Step 5 이후에야 완성될 수 있다. 마이그레이션이 하나 더 필요하다.

**2. AI 토큰 사용량 스위치가 지금은 환경변수다.** `AI_MODE=off|seed|full` 을 `.env` 에서 읽어
**프로세스 기동 시 고정**된다. 관리자가 화면에서 끄고 켜려면 런타임에 바뀌어야 하므로
Redis(또는 DB)에 두고 `AiBudget` 이 매번 읽도록 바꿔야 한다. `.env` 는 기본값으로만 남긴다.
→ 실제 코드 변경이며, `strategy/ai_budget.py` 와 `config.py` 를 건드린다.

**3. 등급별 차등은 화면이 아니라 API 에서 막아야 한다.** 지금 `/signals/latest` 는 인증만
통과하면 전부 내려준다. 화면에서만 숨기면 **API 를 직접 호출해 우회**할 수 있다(유료 신호가
공짜로 나간다). 17-1 의 7개 항목 전부가 서버 측 필터여야 한다:
   - 거래소 커버리지 → `data_sources` 를 등급에 맞게 잘라서 내려준다
   - 신호 지연 → FREE 는 `created_at <= NOW() - 15분` 인 것만
   - 지표·AI 설명 → 응답 필드 마스킹
   - 히스토리 → 조회 가능한 기간 제한 (FREE 없음 / BASIC 7일 / PRO 전체)
   - 백테스트 실행·웹훅 발급 → PRO 만 (지금 `POST /backtest/run` 과 웹훅 발급에 등급 검사가 없다)

**4. `subscriptions` 테이블은 있지만 아무도 읽지 않는다.** 등급 판정 헬퍼가 없다.
`plan`(free/basic/pro) + `status`(active/canceled/expired) + `ends_at` 을 함께 봐야 하고,
만료된 구독은 FREE 로 떨어뜨려야 한다. Step 6-b 의 `Guard` 옆에 붙이는 것이 자연스럽다.

### 진행 순서에 미치는 영향

- **6-b**: `Guard` 에 등급 판정 추가, 백테스트 실행·웹훅 발급에 PRO 검사.
- **9-a**: 유저 화면은 등급 차등을 반영해 구성(잠긴 항목은 숨기지 말고 **잠금 표시** — 업그레이드 유인).
- **9-b**: 관리자 화면 = 유저 관리 + AI 스위치 + 안전장치. **매매 내역 위젯은 Step 5 이후**에 채운다.
- **5**: 체결 내역 테이블 설계가 Step 5 산출물에 포함된다.

## 순서 변경 — 화면을 실거래보다 먼저 (2026-09-03 결정)

**Step 5(실거래)를 Step 6·9(API·화면) 뒤로 옮긴다.** 원래는 5 → 6 → 7 → 8 → 9 순이었다.

**Why:** Step 5 의 핵심은 기능이 아니라 **안전장치**다(kill switch, 일일 손실 한도,
PAPER/LIVE 전환 감사 로그). 그런데 그 상태를 볼 화면이 없으면 **안전장치가 작동했는지조차
알 수 없다.** kill switch 가 켜졌는지, 왜 켜졌는지, 지금 PAPER 인지 LIVE 인지를 SSH 로
DB 를 열어야만 확인할 수 있는 상태로 실거래를 돌리는 것은 위험하다.
Step 6 DoD 에 `GET|PATCH /safety/state` 가 들어 있는 것도 같은 이유다.

**부수 효과가 오히려 안전하다.** `trading_safety_state` 테이블은 이미 있으므로,
화면과 API 를 먼저 만들면 **스위치가 먼저 놓이고 기계(Step 5 매매 엔진)가 나중에 그것을
읽는** 순서가 된다. 반대 순서보다 안전하다.

일정 손해는 없다 — 9/3 에 Step 0~4 를 끝내 원래 9/8 분량까지 처리했고 약 5일 앞서 있다.

### 범위 확대 — 인증과 역할 (2026-09-03 사용자 지시)

대시보드에 **로그인/로그아웃**과 **유저/관리자 역할 분리**를 포함한다. 관리자에게는
실거래 제어 권한을 준다.

- `users.role` ENUM('user','admin') 과 `users.locale` 은 이미 있다. 새 컬럼이 필요 없다.
- `subscriptions.plan` ENUM('free','basic','pro') 도 있다 — PRO 전용 기능(트레이딩뷰 웹훅,
  Step 3)의 노출 판정에 그대로 쓴다.
- **JWT 검증은 Step 3-a 에서 이미 당겨왔다.** 남은 것은 발급(로그인)·폐기(로그아웃)·역할 가드다.
- **로그아웃은 JWT 가 무상태라 그냥은 안 된다.** 실거래 권한이 걸린 관리자 토큰을
  "클라이언트에서 지웠으니 끝"으로 둘 수 없다. Redis 폐기 목록(denylist)에 토큰 식별자를
  만료 시각까지 올려 서버가 거부하게 한다.
- **감사 로그 테이블이 스키마에 없다.** Step 5 DoD 는 "LIVE 전환은 명시적 조작 + 감사 로그"를
  요구하는데 `init.sql` 6개 테이블에 해당하는 것이 없다. → **마이그레이션 004 가 필요하다**
  (누가·언제·무엇을 PAPER→LIVE 로 바꿨는지, kill switch 를 누가 해제했는지).

### 진행 순서

1. **Step 6-a** — `GET /signals/latest`·`/signals/strong`·`/market/summary`,
   `POST /backtest/run`, `GET|PATCH /safety/state`. 공통 에러 포맷은 Step 3-a 의
   `App\Http\Response` 를 그대로 쓴다.
2. **Step 6-b** — 로그인(JWT 발급)·로그아웃(Redis denylist)·역할 가드·Rate Limit(분당 60회).
   마이그레이션 004(감사 로그)도 여기서 만든다.
3. **Step 9-a** — 로그인 화면, 신호 목록·상세(합의율 표시), 백테스트 결과 목록.
   **기준별로 갈라서 보여준다**(GLOBAL_CONSENSUS / upbit 를 한 표에 섞지 않는다).
4. **Step 9-b** — 관리자 화면(PAPER/LIVE 전환, kill switch, 한도 설정), 점수 추이 차트,
   전 화면 하단 법적 고지.
5. **Step 5** — 매매 엔진. 이때는 이미 스위치와 화면이 있다.

## Step 0 — DB 마이그레이션 (신규, 착수 전 필수) — **완료 2026-09-02**

기존 `database/init.sql`(v1 DDL)이 이미 있으므로, 전체 재작성 대신 마이그레이션 스크립트를 추가한다.

**산출물**: `database/migrations/001_v2_multi_exchange.sql`, `database/migrate.sh`(러너),
`database/docker_initdb_migrate.sh`(신규 설치 시 자동 적용), `database/README.md`.
적용 이력은 `schema_migrations` 테이블로 추적한다. MySQL 8.0 컨테이너에서
기존 DB 경로·신규 설치 경로 양쪽으로 적용을 검증했다.

**미결(스펙 충돌, 결정 필요)**: 아래 3건은 `prompt.md` v2 §2 DDL과 기존 스키마가 어긋나
이번 마이그레이션에서 손대지 않았다. 결정 후 `002_*.sql`로 처리한다.
1. `ai_signals` 점수 컬럼 — v1은 `tech_score`/`ai_score`/`risk_score`/`final_score` 4개,
   v2 §2 DDL은 `score` 1개. Step 2 DoD가 Final Score 구성비를 요구하므로 4개를 유지했다.
2. `ai_signal_results` 구조 — v1은 horizon별 행(`UNIQUE(signal_id, horizon)`),
   v2 §2 DDL은 `price_after_5m/15m/1h` 가로 컬럼. Step 7 DoD의
   `INSERT ... ON DUPLICATE KEY UPDATE`는 v1 구조를 전제한다. v1 유지.
3. `trading_safety_state` — v2 §2 DDL에 없으나 Step 5 DoD가
   `kill_switch_active`/`max_position_size_krw`를 요구한다. v1 그대로 유지.

- **DoD**: `exchanges`, `user_webhooks`, `external_signals`(user_id/user_webhook_id 포함) 테이블 신규 생성. `users`에 `locale`(기본값 'ko') 컬럼 추가. `ai_signals`에 `entry_price_global`, `entry_price_upbit`, `bollinger_position`, `stochastic_k`, `stochastic_d`, `adx_val`, `cci_val`, `exchange_consensus_pct`, `data_sources_json` 컬럼 추가하고 기존 `market` 컬럼은 `symbol`로 정리(거래소 무관 심볼). `backtest_logs`에 `reference_exchange` 컬럼 추가. 마이그레이션은 `database/migrations/`에 순번 파일로 관리하며, 기존 데이터가 없는 상태이므로 롤백 스크립트는 생략 가능.

## 스텝별 완료 정의 (DoD, v2)

각 스텝의 상세 요구사항은 `prompt.md`(v2)의 해당 `[Step N]` 프롬프트를 원본으로 삼는다.
아래는 "끝났다"고 말하기 위한 최소 조건이다.

- **Step 1** — `.env`의 `EXCHANGES`(기본: binance,okx,bybit,coinbase,upbit)와 `MARKETS`(기본 5개: BTC/ETH/XRP/SOL/DOGE)로 지정한 조합의 ticker·orderbook(지원 거래소만)이 거래소별로 Redis에 실시간 적재되고, 거래소별 RSI(14)/MACD(12,26,9)/MA(5,20,60) 정배열/Bollinger(20,2)/Stochastic(14,3)/ADX(14)/CCI(20)/호가 불균형이 계산된다. 심볼별 거래량 가중 평균가/지표가 `global:{symbol}:*` 키로 별도 산출된다. 거래소별 연결이 끊기면 해당 거래소만 지수 백오프로 재접속하고 다른 거래소 수집에는 영향이 없다.
- **Step 2** — `S_Consensus`가 prompt.md §3.2대로 산출되고, 유효 거래소 3개 미만이면 HOLD로 강등된다. `S_Tech`가 §3.3 배점표대로 산출되고 `min(sum, 100)`으로 clamp된다. Tech 70 이상/30 이하 **그리고** Consensus 50% 이상일 때만 OpenAI를 호출한다. Redis TTL 키로 중복 호출을 막는다. Final Score(Tech 45%+Consensus 20%+AI 20%+Risk 15%)가 계산되고 확률 합계가 95~105를 벗어나면 HOLD로 강등된다. 결과가 `ai_signals`(신규 컬럼 포함)에 저장되고 `channel:signals`로 publish된다.
- **Step 3** *(PRO 기능, 멀티테넌트)* — 로그인 유저가 `POST /api/v1/webhooks/tradingview`로 고유 웹훅 URL(토큰 포함)을 발급받을 수 있다. `POST /webhook/tv/{token}`이 존재하지 않거나 폐기된 토큰이면 404를 반환한다. 수신 payload가 해당 유저의 `user_id`/`user_webhook_id`와 함께 `external_signals`에 저장된다. 동일 `symbol`의 최근 `ai_signals`가 있으면 `linked_signal_id`로 참고 연결만 하고 점수를 소급 변경하지 않는다. 유저 A의 신호가 유저 B 대시보드에 노출되지 않음을 검증하는 테스트가 있다. 이 기능을 비활성화하거나 유저가 미사용이어도 Step 1·2가 정상 동작한다.
- **Step 4** — 신호검증용(`reference_exchange='GLOBAL_CONSENSUS'`)과 업비트 실전용(`reference_exchange='upbit'`) 백테스트가 분리되어 실행되고, 결과가 하나의 지표로 합쳐지지 않는다. 업비트 실전용에는 수수료 0.05%(매수·매도 각각), 슬리피지 0.05%가 반영된다. 총수익률/승률/거래수/손익비/MDD를 내고 `backtest_logs`에 저장한다. 재현성 테스트 1개 이상.
- **Step 5** — 기본 모드가 **PAPER**다. LIVE 전환은 명시적 조작 + 감사 로그를 남긴다. 일일 손실 한도 초과 시 `kill_switch_active=1`로 신규 주문을 자동 차단하고, 수동 해제 전까지 유지한다. 단일 주문이 `max_position_size_krw`를 넘지 않는다. 기동 시 Upbit 키에 출금 권한이 없는지 검증한다. 매매 대상은 업비트 상장 종목의 `ai_signals`로 한정되고, 체결/잔고 조회는 반드시 업비트 API로만 수행한다.
- **Step 6** — `GET /signals/latest`(페이지네이션, `exchange_consensus_pct`/`data_sources_json` 포함), `GET /signals/strong`, `GET /market/summary`(글로벌 가중 평균가 포함), `POST /backtest/run`(`reference_exchange` 파라미터), `GET|PATCH /safety/state`가 동작한다. 공통 에러 포맷, JWT 인증, 분당 60회 Rate Limit, 전 쿼리 Prepared Statement.
- **Step 7** — 미평가 건만 골라 `INSERT ... ON DUPLICATE KEY UPDATE`로 기록한다. **정답 정의는 `strategy/labeling.py`의 삼중 배리어 하나뿐이며 백테스트와 공유한다**(익절 +5% / 손절 -2.5% / 시간 제한). horizon 5m·15m·1h·4h·1d 각각에 대해 어느 배리어에 먼저 닿았는지를 `exit_reason`으로 남기고, 시간 제한은 부호로 판정한다. HOLD는 제외. Redis 분산 락으로 중복 실행에도 안전하다.
- **Step 8** — RuleEngine clamp(100 상한), Consensus 계산, 백테스트, 성과 추적에 pytest 단위 테스트가 있다. `ci.yml`의 "exit 5 우회"를 제거한다. Python·PHP 양쪽 JSON 구조화 로깅과 에러 알림 훅을 넣는다.
- **Step 9** — 브라우저에서 신호 목록·상세와 거래소 합의율(Consensus %)을 보고, 마켓별 점수 추이 차트를 확인할 수 있다. Step 6의 API만 호출하며 DB에 직접 붙지 않는다. 모든 화면 하단에 prompt.md §7의 법적 고지(트레이딩뷰 공식 데이터가 아니라는 문구 포함)를 노출한다. (prompt.md의 Step 1~7 범위 밖. 9/2 논의에서 추가 합의.)
- **Step 10** — `SUPPORTED_LOCALES` 설정에 언어 코드를 추가하고 `lang/{code}.json` 파일 하나만 채우면 코드 수정 없이 새 언어가 노출된다. `ko/en/ja` 3개 언어로 UI 전체(메뉴/버튼/에러 메시지/법적 고지)가 번역되어 있다. 유저가 마이페이지에서 언어를 변경하면 `users.locale`이 갱신되고 다음 로그인부터 유지된다. AI 신호의 `reasons`/`risks`를 기준 언어와 다른 `locale`로 볼 때 자동 번역되며 "AI 자동 번역" 배지가 표시된다. 법적 고지는 언어별 고정 문구이며 실시간 번역을 거치지 않는다.

## Step 11·12 — 외부 데이터 아카이브 (2026-09-02 추가 합의)

`prompt.md` v2 범위 밖의 확장. 상세 설계는 **`docs/EXTERNAL_DATA.md`**.

- **Step 11 (수집)** — 코인 뉴스 RSS + 매크로 시계열(FRED/美재무부) + 경제 캘린더를
  아카이브한다. **신호 점수에는 반영하지 않는다.** 목적은 아카이브 축적이다 —
  과거 데이터는 지금 모으지 않으면 소급 확보가 안 된다.
  - DoD: 뉴스/매크로/캘린더가 각각의 테이블에 적재되고, 모든 레코드가
    "사건 시각"과 "우리가 본 시각"을 분리 보관한다. 매크로는 개정(revision)을 덮어쓰지 않는다.
    `external/point_in_time.py`의 `as_of(t)`가 t 시점에 알 수 있었던 것만 돌려주고,
    이를 검증하는 테스트가 있다. 비공식 스크래핑을 쓰지 않는다.
- **Step 12 (뉴스 메뉴 + 커뮤니티)** — 기사 목록·상세(원문 링크 아웃)·상승/하락 투표·댓글.
  Step 6 API와 Step 9 UI 위에 얹는다.
  - DoD: 원문 본문을 재배포하지 않고 제목·요약·링크만 노출한다. 유저 투표가
    `article_sentiments`에 `classifier='crowd'`로 집계되어 AI 분류와 나란히 비교된다.
    유저는 1기사 1표이며 변경할 수 있다.

**편입 판단은 Step 11·12가 아니라 데이터가 쌓인 뒤에 한다.** 상관관계가 없으면
점수에 넣지 않는 것도 결과다. 넣기로 하면 `Final Score` 재배분(Tech 45/Consensus 20/
AI 20/Risk 15)이 필요하므로 `prompt.md` §3.3 개정이 선행되어야 한다.

**사용자 액션 필요**: FRED 무료 API 키 발급 (https://fred.stlouisfed.org/docs/api/api_key.html).
연준금리·CPI·유가·달러지수와 과거 시점 데이터(ALFRED)를 한 키로 커버한다.

## 적중률 정의 통일 — 삼중 배리어 (Step 7 선행 작업, 완료)

**두 곳이 서로 다른 것을 재고 있었다.**

    백테스트   익절 +5% / 손절 -2.5% 중 어디에 먼저 닿았나 (삼중 배리어)
    적중률     5분 뒤 종가가 진입가 대비 ±0.2% 를 넘었나

같은 신호가 한쪽에서는 성공, 다른 쪽에서는 실패로 기록될 수 있었다. 그러면
"백테스트는 좋아졌는데 적중률은 그대로"인 상황에서 무엇을 믿을지 알 수 없다.

**±0.2% 는 애초에 잘못된 정답 정의다.** 왕복 수수료(신호검증용 0.18% / 업비트 0.20%)를
빼면 기댓값이 0 에 수렴한다 — 맞춰도 남는 게 없는 정답을 쫓는 셈이다.
실제 매매는 익절선·손절선을 걸고 기다리는 것이므로 라벨도 그 모양이어야 한다.

### 산출물

- **`strategy/labeling.py`** — 정답 정의가 사는 단 하나의 파일. 익절·손절 상수를
  **`BacktestParams` 가 여기서 가져다 쓴다** (두 곳이 갈라질 수 없게).
- **마이그레이션 005** (로컬·서버 적용 완료) — `horizon` ENUM 에 `4h`·`1d` 추가,
  `exit_reason`·`best_price`·`worst_price` 컬럼 추가.
- 테스트 14건 (`tests/test_labeling.py`). 전체 245건.

### 정한 것

1. **horizon 을 늘리되 기존 값을 지우지 않는다.** 배리어까지 걸린 시간이 곧 "얼마나 들고
   있어야 하는가"다. 짧은 horizon 이 대부분 `TIME_LIMIT` 으로 끝난다면 **그 신호가 단기
   매매용이 아니라는 사실이 데이터로 드러나야 한다.** 5m·15m 를 남긴 이유다.
2. **시간 제한은 미결이 아니라 부호로 판정한다.** 미결로 두면 "안 끝난 거래"가 통계에서
   통째로 빠져 승률이 왜곡된다. `exit_reason` 을 함께 남기므로 "익절 도달 기준 승률"과
   "부호 기준 승률"을 둘 다 낼 수 있다.
3. **한 봉에서 익절·손절에 모두 닿으면 손절이 먼저**다 (백테스트와 같은 규칙).
   OHLC 로는 순서를 알 수 없고, 유리한 쪽을 가정하면 통계가 부풀려진다.
4. **`best_price`/`worst_price` 를 남긴다.** 배리어에 얼마나 근접했는지가 있어야
   폭을 재보정할 때(Step 16) 근거가 된다.

### 타임프레임 불일치 — **결정 완료 (2026-09-04, Step 16)**

운영 엔진은 **5분봉**으로 도는데(`CANDLE_TIMEFRAME`), 익절 5% / 손절 2.5% 는
**1시간봉 탐색**에서 고른 값이었다. 5분봉에서 검증한 적이 한 번도 없었고, 측정해 보니
**익절 도달이 0건**이었다 — 청산의 90% 이상이 신호 청산으로 일어났다.

두 안 중 **(a) 를 먼저 측정하기로 했던 약속대로** 5분봉에서 좁은 배리어를 재탐색했다
(14일, BTC·ETH·SOL, 임계값 80).

| 익절/손절 | 총 거래 | 평균 수익률 | 익절 도달 |
| --- | ---: | ---: | ---: |
| 0.8 / 0.4 | 613 | **-27.74%** | 143 |
| 1.2 / 0.6 | 447 | -17.48% | 75 |
| 2.0 / 1.0 | 316 | -12.63% | 33 |
| 3.0 / 1.5 | 243 | -5.60% | 15 |
| 5.0 / 2.5 (현행) | 205 | **-2.40%** | 3 |

**(a) 는 기각한다.** 폭을 좁힐수록 결과가 **단조롭게 나빠진다.** 좁히면 익절 도달은
늘지만(3건 → 143건) 손절이 훨씬 더 빨리 늘고, 무엇보다 거래 수가 3배가 되어 왕복
비용이 그만큼 곱해진다.

#### 그런데 진짜 원인은 배리어가 아니었다

비용을 뺀 손익과 넣은 손익을 분리해 보니 다른 그림이 나왔다 (5분봉 14일, 3심볼 평균):

| 진입 임계값 | 거래 | 순수익 | **무비용 수익** | 비용 |
| ---: | ---: | ---: | ---: | ---: |
| 80 | 205 | -2.79% | **+9.95%** | -12.74% |
| 85 | 163 | -0.70% | **+9.51%** | -10.21% |
| 90 | 80 | -6.35% | -1.74% | -4.61% |
| 95 | 8 | -0.22% | +0.26% | -0.48% |

**전략에 엣지가 없는 게 아니다. 엣지보다 비용이 크다.**
거래당으로 환산하면 무비용 손익이 **0.146% (임계값 80) / 0.176% (85)** 인데
**왕복 비용이 0.18%** 다. 간발의 차로 매번 진다.

배리어 폭을 어떻게 잡아도 이 관계는 바뀌지 않는다. 폭을 넓히면 거래가 줄어 비용도
줄지만 엣지도 같이 준다. **고쳐야 할 것은 거래당 기대값이다.**

#### 결론 — 5분봉을 유지한다

- **(a) 좁은 배리어: 기각.** 측정 결과 단조롭게 나빠졌다.
- **(b) 1시간봉 전환: 보류.** 14일 1시간봉은 거래가 2~9건뿐이라 판단 근거가 되지 못했고,
  그 표본에서도 전부 손실이었다. 5분봉보다 낫다는 증거가 **없다.**
- **현행 5분봉 · 익절 5.0 / 손절 2.5 유지.** 근거 없이 바꾸지 않는다.
  `prompt.md` §4 의 "short-term (5m, 15m)" 문구도 그대로 둔다.

#### 배점표 v3 적용 후 다시 잰 타임프레임별 거래당 기대값

배점표를 고친 뒤 같은 질문을 다시 던졌다. **핵심 지표는 수익률이 아니라 거래당 무비용
손익**이다 — 왕복 비용 0.180% 를 넘지 못하면 거래를 많이 하든 적게 하든 결국 진다.

| 봉 | 기간 | BTC | ETH | SOL |
| --- | ---: | ---: | ---: | ---: |
| 5m | 14일 | 0.220% | **0.090%** | 0.477% |
| 15m | 45일 | 0.318% | 0.242% | 0.851% |
| 1h | 90일 | 0.461% | **0.022%** | 0.903% |
| 4h | 180일 | 1.376% | **0.148%** | **-0.126%** |

느린 봉일수록 거래당 기대값이 커지는 경향은 보인다(거래당 움직임 자체가 크므로 당연하다).
**그러나 타임프레임을 바꾸지 않는다.**

- 각 칸이 **거래 13~43건**짜리 단일 구간이다. 앞서 세 구간 검증에서 확인했듯 이 정도
  표본은 시기를 옮기면 부호가 뒤집힌다. 한 구간으로 운영 타임프레임을 옮기는 것은
  v3 에서 하지 않기로 한 바로 그 과최적화다.
- ETH 는 모든 봉에서 비용을 못 넘었고, 4시간봉 SOL 은 음수다. **일관되게 나은 봉이 없다.**

→ **5분봉 유지.** 타임프레임 재검토는 Step 7 의 실데이터가 쌓이고 **Step 17(워크포워드)**
에서 여러 구간에 걸쳐 볼 수 있게 된 뒤에 한다.

**Step 7 의 5m·15m horizon 은 거의 항상 `TIME_LIMIT` 으로 기록될 것이다.** 이는 버그가
아니라 사실의 기록이다 — "이 신호는 5분짜리가 아니다"가 데이터로 드러나야 한다.

---

## Step 7 — 시그널 성과 자동 추적 스케줄러 — **완료 2026-09-04**

`trading_engine/tracking/result_tracker.py` + systemd 유닛 `ai-trading-scheduler`.

### 정답 정의는 `labeling.py` 하나뿐이다

판정을 직접 하지 않고 전부 `strategy/labeling.py` 에 위임한다. **백테스트가 쓰는 그
모듈이다.** 스펙 원안의 "5분 뒤 ±0.2%" 는 쓰지 않았다 — 왕복 수수료(0.18~0.20%)를 빼면
기댓값이 0 에 수렴해서, 맞춰도 남는 게 없는 정답을 쫓게 된다.

한 신호를 horizon 5개(5m·15m·1h·4h·1d)로 **각각** 평가한다.

### 부풀려지지 않게 하려고 넣은 규칙 넷

적중률은 이 서비스가 광고할 수 있는 유일한 실적이라(`docs/LEGAL.md`), 재는 방식이 한
군데라도 느슨하면 대외적으로 말할 수 있는 내용 전체가 무너진다.

1. **신호가 걸쳐 있는 봉은 뺀다.** 그 봉의 고가·저가에는 신호가 나기 **전**의 움직임이
   섞여 있다. 그대로 쓰면 신호를 내기도 전에 닿았던 가격으로 익절이 판정된다.
   → `ts > created_at` 인 봉만 본다.
2. **시간 제한이 다 지나기 전에는 기록하지 않는다.** 미리 쓰면 아직 닿을 수 있는
   배리어를 놓치고 `TIME_LIMIT` 으로 굳는다. horizon + **봉 하나**를 더 기다린다
   (마지막 봉이 닫혀야 고가·저가가 확정된다).
3. **한 봉에서 익절·손절에 모두 닿으면 손절이 먼저다.** 백테스트와 같은 규칙이다.
4. **HOLD 는 평가하지 않는다.** 진입하지 않았으므로 맞고 틀리고가 없다. 세면 승률이
   통째로 왜곡된다. SQL 단계에서 뺀다 — 매 주기 뽑아서 버리면 배치만 낭비한다.

### 가격은 백테스트와 같은 함수로 만든다

Redis 에는 최신 시세만 남으므로 거래소에서 캔들을 다시 받아 `backtest.data.
global_price_series()` 로 글로벌 거래량 가중 평균 계열을 만든다. 여기서만 다른 가격을
쓰면 "백테스트는 좋은데 적중률은 나쁘다"가 나와도 원인을 가릴 수 없다.
심볼별로 대기 건의 구간을 **합쳐서 한 번만** 받아온다 (건별로 받으면 같은 캔들을 수십 번
다시 받는다).

### 중복 실행 안전장치 (DoD)

- Redis 분산 락 `lock:signal-result-tracker` — TTL 필수(잡은 프로세스가 죽으면 아무도
  풀어줄 수 없다). 해제는 **내 토큰일 때만** Lua 로 비교·삭제한다. GET 후 DEL 로 나누면
  그 사이 TTL 이 만료돼 남의 락을 지울 수 있다.
- `INSERT ... ON DUPLICATE KEY UPDATE` — 락이 만료된 채 두 인스턴스가 겹쳐 돌아도
  결과가 같다. **정합성을 지키는 건 락이 아니라 이쪽이다.**

### 엔드투엔드 검증 (로컬, 실제 BTC 캔들)

실제 시점의 글로벌 가중평균가를 진입가로 넣은 합성 신호 4건(BUY/SELL/HOLD/STRONG_BUY):

| 확인 | 결과 |
| --- | --- |
| 비 HOLD 3건 × horizon 5개 | **15행 기록** |
| HOLD | **0행** (평가 대상 아님) |
| 2회차 실행 | **0건 추가**, 15행 유지 (멱등) |
| 락 보유 중 재실행 | 건너뜀 |

**15행 전부 `TIME_LIMIT` 이었다.** 예상대로다 — 5분봉 신호에 익절 5% / 손절 2.5% 는
거의 닿지 않는다(Step 16 참고). **버그가 아니라 사실의 기록**이고, 배리어 폭을 다시
잡을 근거(`best_price`/`worst_price`)가 여기서부터 쌓인다.

### 배점표가 다른 신호를 섞지 않는다 (2026-09-04 추가)

배점표 v3 은 **2026-09-04 08:52:30 KST**(엔진 재시작)부터 적용됐다. 그 전후 신호를
한 통계에 넣으면 "고쳤더니 나아졌는가"를 영원히 알 수 없다.

날짜를 코드 곳곳에 박는 대신 **신호에 배점 체계 버전을 새긴다**:

- `strategy/versioning.py` 의 `SCORING_VERSION` 을 엔진이 저장 시 함께 쓴다
  (`ai_signals.scoring_version`, `data_sources_json` 양쪽).
- 마이그레이션 006 이 기존 24건을 `v2` 로 소급 분류했다.
  **날짜가 쓰인 곳은 그 마이그레이션 하나뿐이다** — 다음에 배점표를 고칠 때 날짜를
  다시 찾을 필요가 없다.
- 컬럼 기본값이 `'v3'` 이라 마이그레이션과 코드 배포 사이에 들어온 신호도 올바르게
  기록된다(그 시점의 엔진이 이미 v3 이므로).

**적중률은 반드시 `GROUP BY scoring_version` 으로 본다** (표준 조회는 `deploy/README.md`).

버전을 올려야 하는 경우: 배점표, 최종 가중치, 등급 임계값, 방향 임계값, 위험 점수 중
**하나라도** 바뀔 때. 지표를 계산만 하고 점수에 넣지 않는 것은 대상이 아니다.

### 적중률 화면 — **관리자 전용** (2026-09-04 추가)

`GET /api/v1/admin/accuracy` + 대시보드 `#/accuracy`.

**유저에게는 노출하지 않는다.** 관리자가 아니면 403 이 아니라 **404** 다 — 403 은
"그 경로는 존재한다"를 알려준다. 내비게이션에도 관리자에게만 그린다.

법률 검토(`docs/LEGAL.md`)는 **고객에게 보여주는 표현**을 규율한다. 지금 화면은
운영자 본인이 이 도구를 신뢰할 수 있는지 판단하기 위한 내부 도구이므로 대상이 아니다.
**유저 노출로 전환할 때 그때 검토한다.**

화면이 보여주는 것 — 좋아 보이게 꾸미지 않고 **불리한 숫자를 먼저 보여준다**:

| 표 | 왜 |
| --- | --- |
| 배점표 × 시간제한 | **이 화면의 존재 이유.** v2·v3 을 절대 섞지 않는다 |
| 점수 구간별 | "점수가 높을수록 좋은 신호인가" — 백테스트에서는 **아니었다.** 운영에서도 그런지 상설 관찰 |
| 심볼별 / 신호 종류별 | 어디서 되고 어디서 안 되는지 |
| 최근 개별 결과 | 집계만 보면 무엇이 이상한지 알 수 없다 |
| 청산 사유 비중 막대 | **시간초과가 대부분이면 그 적중률은 "방향만 맞았다"는 뜻**이다 |

표본 30건 미만은 회색 처리하고 "통계로 읽지 마세요"를 붙인다. 화면 하단에 왕복 비용
0.18% 를 못 넘으면 실거래에서는 손실이라는 주의를 상설로 둔다.

### 그 과정에서 고친 것 — 화면 시각이 전부 "방금" 이었다

`when()` 이 DB 시각에 `'Z'` 를 붙여 UTC 로 읽고 있었다. **이 서버는 PHP 가 UTC,
MySQL 이 KST** 라 9시간이 어긋나 모든 시각이 미래가 됐고("-490분 전") 화면에는 전부
"방금"으로 나왔다. 접미사를 떼어 브라우저 로컬 시각으로 읽게 고쳤다.

같은 이유로 **적중률 조회의 기간 하한은 PHP 가 아니라 DB 시계로 만든다**
(`AccuracyRepository::since()`). PHP 에서 계산해 넘기면 최근 9시간이 통째로 빠진다.

> **Step 10(다국어) 때 정리할 것**: API 가 시간대까지 담은 ISO8601 로 주고 화면은
> 그것만 믿게 해야 다른 시간대의 접속자에게도 맞는다.

## Step 16 — 배점표 재설계 — **완료 2026-09-04**

### 먼저 잰 것: 배점표가 품질을 가르는가

5분봉 14일, 5심볼, **표본 9,443건**. 각 신호에 삼중 배리어 결과(비용 제외)를 붙였다.

| Final Score | 건수 | 평균 손익 | 승률 |
| --- | ---: | ---: | ---: |
| 60~69 | 378 | +0.085% | 45.5% |
| 70~79 | 4,739 | +0.256% | 48.8% |
| 80~89 | 4,120 | +0.439% | 50.6% |
| 90~100 | 206 | +0.423% | 51.0% |

점수가 **거의** 순위를 매기지만 90점 위에서 무너진다. 항목별로 뜯어보니 이유가 나왔다.

| 배점 항목 | 켜졌을 때 | 꺼졌을 때 | 차이 |
| --- | ---: | ---: | ---: |
| RSI | +0.312% | +0.975% | **-0.663%** |
| MA 정배열 | +0.583% | +0.104% | +0.479% |
| MACD 골든크로스 | +0.555% | +0.317% | +0.238% |
| 스토캐스틱 | +0.156% | +0.382% | **-0.226%** |
| 볼린저 | +0.134% | +0.351% | **-0.217%** |
| ADX | +0.451% | +0.277% | +0.174% |
| CCI | +0.252% | +0.354% | **-0.102%** |
| 거래량 | +0.370% | +0.302% | +0.068% |
| 호가 불균형 | — | — | **표본 0건** |

**절반의 배점(RSI 20 + 스토캐스틱 10 + 볼린저 10 + CCI 10 = 50점)이 결과와 반대
방향으로 움직였다.** 그리고 호가 불균형 10점은 백테스트에 호가 데이터가 없어
**9,443건 중 0건에서만 켜졌다** — 한 번도 검증된 적 없는 점수였다.

### 그런데 표본 외에서는 그마저 무너진다 (가장 중요한 발견)

같은 진단을 시기를 옮겨 세 번 돌렸다 (각 5,700여 표본, BTC·ETH·SOL).

| 항목 | 최근 14일 | 28~42일 전 | 56~70일 전 |
| --- | ---: | ---: | ---: |
| MA 정배열 | +0.490% | +0.003% | +0.101% |
| MACD | +0.278% | -0.041% | +0.193% |
| ADX | +0.090% | +0.068% | **-0.033%** |
| 스토캐스틱 | -0.543% | +0.007% | -0.467% |
| 볼린저 | -0.315% | **+0.032%** | +0.007% |
| CCI | -0.022% | -0.094% | **+0.197%** |

**RSI 는 아예 정반대로 뒤집혔다.** 최근 14일에는 과매도(만점)가 가장 나빴고(-0.029%),
56~70일 전에는 과매도가 가장 좋았다(+0.591%).

→ **실측 수치에 맞춰 배점을 다시 쓰는 것은 불가능하다.** 맞춰 쓸 안정적인 신호가 없다.
최근 구간에 맞춰 RSI 를 뒤집었다면 두 달 전 국면에서 정확히 반대로 작동했을 것이다.
이것이 과최적화의 교과서적 사례다.

### 그래서 무엇을 고쳤는가 — **숫자가 아니라 구조**

임계값(RSI 30/70, ADX 25, 거래량 30%)은 **하나도 바꾸지 않았다.** 고친 셋은 모두
데이터와 무관하게 틀린 것들이다.

**① 비대칭 — 하락 근거를 점수로 표현할 수 없었다.**
가점 +110 / 감점 -30. MACD 는 골든크로스에 +15 를 주면서 **데드크로스는 계산조차
하지 않았고**(`macd_golden_cross` bool 하나뿐), MA 정배열 +15 에 역배열은 0점이었다.
그래서 중립 시장이 10점이 되었고, §3.2 의 60/40 과 맞추려고 **기준점 40 을 얹는 보정**이
필요했다. → 모든 항목을 0 중심 대칭으로 만들고 기준점을 **50** 으로. 보정이 사라졌다.

**② 중복 — 같은 것을 세 번 셌다.**
RSI·스토캐스틱·CCI 실측 상관 r = 0.76~0.84 인데 합쳐서 40점이었다.
→ **모멘텀 축을 RSI 하나(±10)로 합쳤다.**

**③ 검증되지 않은 배점.** 호가 불균형 10점 → 점수에서 뺐다.

### 배점표 v3

| 항목 | 배점 | 판정 |
| --- | ---: | --- |
| 기준점 | **50** | 중립 |
| 모멘텀 (RSI) | **±10** | 30 이하 +10 / 70 이상 −10 / 50 이 0 |
| 추세 배열 (MA) | **±15** | 정배열 +15 / 역배열 −15 |
| 추세 강도 (ADX) | **±10** | 25 이상이면 MA 방향으로 ±10 |
| MACD 교차 | **±10** | 골든 +10 / **데드 −10** (신규) |
| 볼린저 위치 | **±5** | 하단 복귀 +5 / 상단 돌파 지속 −5 |
| 거래량 급증 | **±5** | MA 방향으로 ±5 (v2 는 무조건 +10 이었다) |
| 스토캐스틱 · CCI · 호가 · VWAP | 0 | **참고 표시** — 화면에는 남기고 점수만 뺀다 |

점수에서 뺀 항목도 0점짜리 내역으로 남긴다. 지워버리면 왜 뺐는지도 사라지고,
다시 넣을지 판단할 근거(값 자체)가 쌓이지 않는다.

### 채택 판정 — 같은 데이터에서 v2 와 나란히 돌렸다

v3 은 어느 구간에도 맞추지 않았으므로 "좋아졌다"를 증명할 의무는 없다. 그러나
**나빠지지 않았다**는 것은 확인해야 채택할 수 있다. 세 구간 × 3심볼, 같은 캔들,
같은 파이프라인에서 배점표만 갈아 끼워 비교했다.

| 구간 | v2 순수익 | **v3 순수익** | v2 거래 | v3 거래 |
| --- | ---: | ---: | ---: | ---: |
| 최근 14일 (상승) | -2.43% | **+3.30%** | 205 | 105 |
| 28~42일 전 (횡보) | -9.75% | **-7.85%** | 186 | 103 |
| 56~70일 전 (상승) | **-3.61%** | -6.48% | 175 | 106 |
| **전체 평균** | -5.27% | **-3.68%** | 566 | **314** |

**→ 채택한다.** 3구간 중 2구간에서, 그리고 전체 평균에서 v3 이 낫다.

**다만 무엇 때문에 나아졌는지는 분명히 해 둔다.** 순수익이 좋아진 이유는 신호가
좋아져서가 아니라 **거래를 44% 덜 해서**다 (566 → 314건, 비용 그만큼 감소).
비용을 뺀 무비용 수익은 오히려 **+6.10% → +2.57% 로 줄었다.** 승률도 44% → 30% 로 낮다.

이유는 v3 이 **하락을 말할 수 있게 되었기 때문**이다. 백테스트는 SELL 신호에서 청산하는데
(`EXIT_SIGNALS`), v2 는 하락 근거에 점수를 줄 수 없어 SELL 이 거의 안 나왔다. v3 은
데드크로스·역배열에 감점을 주므로 더 일찍 빠져나온다. 표본 구간이 상승에 기울어 있어
**일찍 빠져나오는 것이 손해로 보이는 것**이고, 실제로 횡보 구간(28~42일 전)에서는
v3 이 세 심볼 모두 앞섰다. 하락장에서는 이 방향이 보호로 작동한다.

**두 배점표 모두 점수가 품질을 제대로 가르지 못한다.**

| Final Score | v2 평균 손익 | v3 평균 손익 |
| --- | ---: | ---: |
| 70~79 | +0.213% | +0.028% |
| 80~89 | +0.316% | -0.030% |
| 90~100 | +0.221% | +0.648% (5건) |

v2 는 80점대까지 오르다 90점대에서 무너지고, v3 은 아예 기울기가 없다.
**"점수가 높을수록 좋은 신호"라고 말할 근거가 아직 없다.** 화면·문서에서 점수를
확신도처럼 설명하지 않도록 주의해야 한다 (법률 검토의 "단정적 표현 금지"와도 같은 방향).

AI 게이트 통과율은 20.9% → 20.6% 로 사실상 같다. **비용 구조는 바뀌지 않는다.**

### 국면 분리(ADX)는 넣지 않았다

전략 자문의 제안대로 ADX 로 추세장·횡보장을 나눠 재보니 부호가 갈리기는 했다
(추세장에서 스토캐스틱 +0.290%, 횡보장에서 -0.369%). 하지만 **그 갈림 자체가 시기마다
달랐다.** 지금 국면별 표를 둘로 나누면 조정할 파라미터가 두 배가 되고 과최적화만
심해진다. → **Step 17(워크포워드)에서 국면 분리가 out-of-sample 로 버티는지 확인한 뒤에
넣는다.**

### 남은 문제 — 배점표로는 풀리지 않는다

거래당 무비용 손익 0.15~0.18% vs 왕복 비용 0.18%. **배점표를 어떻게 고쳐도 이 격차는
메워지지 않는다.** 필요한 것은 정보의 양이지 배점의 배분이 아니다.
→ **Step 14 (파생 데이터)** 와 **Step 15 (김치 프리미엄)** 가 그래서 우선순위가 높다.
현물 지표만으로는 "가격 상승 + 신규 롱"과 "가격 상승 + 숏 커버"를 구분할 수 없다.

## Step 14 착수 전 실측 — 파생 데이터 이력의 한계 (2026-09-04)

**"쓸 수 있다"와 "과거를 잴 수 있다"는 다르다.** 예측력을 재려면 이력이 필요하고,
없으면 지금부터 직접 쌓는 수밖에 없다(= 몇 주 대기).

| | 펀딩비 이력 | **미결제약정(OI) 이력** |
| --- | --- | --- |
| binance | 180일+ | **30일** — 60일 요청 시 `BadRequest` |
| **bybit** | 180일+ | **180일+** ✅ |
| okx | 90일+ | **30일** — 60일 요청 시 `ExchangeError` |

Step 16 에서 **단일 구간 측정은 시기마다 부호가 뒤집힌다**는 것을 확인했으므로
3구간 검증(최근 / 28~42일 전 / 56~70일 전)이 필수인데, OI 는 **bybit 만** 그게 된다.

다행히 bybit 는 **5분봉 해상도로도 180일치**가 나온다(운영 타임프레임과 일치).
실측으로 14일 = 4,032건이 **간격 누락 없이**(전부 300,000ms) 수집됐다.

### 그래서 정한 것

- **OI 는 bybit 단일 소스**로 3구간 검증한다. OI 는 원래 시장 전체 포지션 지표라
  단일 거래소도 의미가 있다(가격과 달리 거래소 간 합의를 낼 성질이 아니다).
- **펀딩비는 3거래소 합산**이 가능하다.
- **지금부터 3거래소 OI 를 아카이브해 둔다** — 안 하면 30일 이전은 영영 못 받는다.
- 순서는 **측정 먼저, 수집·배점 반영은 그 다음**이다. Step 16 에서 배운 그대로다.

## Step 17-a — 워크포워드 검증 도구 (2026-09-04)

`trading_engine/validation/`. **검증이 싸져야 실제로 검증한다.**

### 왜 만들었나

같은 함정에 두 번 빠졌고 두 번 다 즉석 스크립트로 겨우 피했다.

- **Step 16**: 최근 14일에서 RSI 과매도가 가장 나빴는데 두 달 전에는 가장 좋았다.
- **Step 14**: "신규 롱 + 펀딩 양수"가 두 구간에서 +0.36% / +1.15% 였는데 나머지
  한 구간에서 -0.234% 였다.

그런데 **3구간으로는 부호가 우연히 맞을 확률이 25%** 다. 피한 것도 사실 운이었다.

### 구성

| 파일 | 역할 |
| --- | --- |
| `windows.py` | 겹치지 않는 구간 생성. **UTC 자정 정렬** — 실행할 때마다 경계가 밀리면 캐시가 매번 무효가 된다 |
| `cache.py` | 캔들 로컬 캐시. 과거 구간은 내용이 바뀌지 않으므로 만료를 두지 않는다 |
| `stability.py` | **부호 검정**으로 판정. 눈대중을 없앤다 |
| `walkforward.py` | 러너 + CLI |

### 판정 규칙 (명시한다)

    구간 8개 미만        → 판단 보류
    부호 검정 p >= 0.05  → 불안정 (시기에 따라 부호가 바뀐다)
    |중앙값| < 왕복 비용  → 효과 미달 (실거래에서 비용에 먹힌다)
    그 외                → 안정

**평균이 아니라 중앙값**을 쓴다. Step 14 에서 한 구간의 +1.15% 가 평균을 통째로
끌어올릴 뻔했다. 부호만 세면 그 구간도 다른 구간과 똑같이 한 표다.

**총수익률이 아니라 거래당 손익**으로 잰다. 총수익률은 "거래를 몇 번 했는가"에
지배된다 — Step 16 에서 배리어를 넓힐수록 총수익이 좋아진 원인이 신호 개선이 아니라
거래 수 감소였다.

### 검증을 싸게 만든 두 가지

**1. 거래소별 병렬 수집** (`backtest/runner.py`). 하나씩 받으면 5거래소 × 페이지네이션이
그대로 더해진다. 실측으로 **한 구간(BTC 14일 5분봉)에 약 4분** 이 걸렸고, 12구간 ×
3심볼이면 수집만 두 시간이다. 거래소마다 호스트도 rate limit 도 다르므로 동시에 받아도
서로를 방해하지 않는다. → **4분 → 7.8초.**

**2. 변형 간 신호 공유.** `compute_signals` 는 익절·손절·진입 임계값과 무관하다.
그래서 한 번 계산해 모든 변형에 쓴다 — 변형을 세 개 물어보는 비용이 하나와 같다.
신호를 바꾸는 항목(`timeframe`·`reference_exchange`)을 `--variant` 로 주면 **거절한다.**
조용히 받아들이면 "같은 신호 위에서 비교했다"는 전제가 깨진 채로 표가 나온다.

### 쓰는 법

```bash
# 지금 전략이 구간마다 어떤 성적인지
python -m trading_engine.validation.walkforward

# 여러 변형을 한 번에 (신호는 한 번만 계산해 공유한다)
python -m trading_engine.validation.walkforward \
    --variant take_profit_pct=3.0,stop_loss_pct=1.5 \
    --variant min_final_score=85
```

`BacktestParams` 에 없는 항목을 쓰면 즉시 실패한다 — 오타를 조용히 무시하면
"바꿨는데 아무 일도 안 일어나는" 검증이 된다.

### 아직 안 한 것 — 신호 전체 공개(해시 봉인)

Step 17 의 나머지 절반이다. **유저에게 보여주는 기능이라 지금은 하지 않는다**
(관리자 우선 방침). 재료는 이미 있다 — `ai_signal_results` 가 틀린 것 포함 전부를
기록하고 있고, 이제 하루 300건 넘게 쌓인다.

## Step 14 측정 결과 — **파생 데이터를 배점에 넣지 않는다** (2026-09-04)

자문의 핵심 주장을 직접 검증했다: *"가격 상승 + OI 증가(신규 롱)"와 "가격 상승 +
OI 감소(숏 커버)"는 현물 지표로 절대 구분되지 않는다.*

3구간 × 3심볼, **표본 18,683건**. OI 는 bybit 5분봉, 변화는 직전 12봉(1시간) 기준.

### 주장은 맞다 — 4분면은 실제로 다른 성적을 낸다

| 4분면 | 최근 14일 | 28~42일 전 | 56~70일 전 |
| --- | ---: | ---: | ---: |
| 상승+OI증가 (신규 롱) | **+0.258%** | -0.002% | **+0.406%** |
| 상승+OI감소 (숏 커버) | +0.160% | +0.051% | +0.006% |
| 하락+OI증가 (신규 숏) | **-0.265%** | +0.045% | **-0.309%** |
| 하락+OI감소 (롱 청산) | -0.229% | -0.077% | -0.181% |

(기준 평균 대비. 신규 롱과 신규 숏의 격차가 두 구간에서 0.52~0.72%p 다.)

### 그런데 그 대부분은 OI 가 아니라 **가격 방향**이다

가격 방향을 통제하고 OI 만의 기여를 재면 **부호가 뒤집힌다.**

| 구간 | 상승 장면 OI↑ − OI↓ | 하락 장면 OI↑ − OI↓ |
| --- | ---: | ---: |
| 최근 14일 | +0.097% | -0.036% |
| 28~42일 전 | **-0.053%** | **+0.122%** |
| 56~70일 전 | +0.399% | -0.128% |

순수 모멘텀 대비 추가 기여도 마찬가지다: +0.062% / **-0.028%** / +0.236%.

### 필터로 써도 마찬가지다 (STRONG_BUY 진입, 왕복비용 0.18% 차감)

| 필터 | 최근 14일 | 28~42일 전 | 56~70일 전 | 3구간 모두 개선 |
| --- | ---: | ---: | ---: | --- |
| 없음 (기준) | +0.118% | -0.169% | +0.128% | — |
| 롱청산 구간 제외 | +0.092% | -0.160% | +0.144% | 아니오 |
| 하락 구간 전체 제외 | +0.102% | -0.136% | +0.106% | 아니오 |
| **신규 롱만** | +0.164% | **-0.164%** | +0.342% | **예** (단, 구간2 개선폭 +0.005%p) |
| 펀딩비 음수 제외 | +0.181% | **-0.250%** | +0.186% | 아니오 |
| 펀딩비 양수만 | +0.329% | **-0.418%** | +0.796% | 아니오 |
| 신규 롱 + 펀딩 양수 | +0.360% | **-0.234%** | +1.152% | 아니오 |

**"신규 롱 + 펀딩 양수"가 구간 1·3 에서 +0.36% / +1.15% 로 극적이지만 구간 2 에서
-0.234% 다.** 두 구간만 보고 채택했다면 정확히 Step 16 이 경고한 과최적화다.

### 결론

1. **배점표에 넣지 않는다.** OI 의 순수 기여는 시기마다 부호가 뒤집힌다 — RSI·볼린저와
   같은 문제다(Step 16).
2. **필터로도 지금은 넣지 않는다.** 3구간 모두 개선인 것은 "신규 롱만" 하나뿐인데,
   개선폭이 한 구간에서 +0.005%p(노이즈)이고 거래 수가 절반 이하로 준다.
3. **어느 조합도 28~42일 구간을 양수로 만들지 못했다.** 최고 조합조차 -0.234% 다.
   **정보를 더해도 비용을 넘지 못하는 국면이 존재한다** — Step 16 의 결론이 강화됐다.
4. **아카이브도 지금은 만들지 않는다.** bybit 가 5분봉 180일치를 주므로 Step 17
   워크포워드에 필요한 구간(비중첩 14일 창 약 12개)은 이미 확보돼 있다.
   binance·okx 의 30일 한계는 그때 필요해지면 다시 본다.

→ **다음은 Step 17(워크포워드 검증)이다.** 3구간으로는 "안정적인가"를 판정할 수 없다는
것이 이제 두 번 확인됐다(Step 16 배점표, Step 14 파생 데이터). 구간을 12개로 늘려
같은 질문을 자동으로 반복할 수 있는 도구가 있어야 이 판단들이 싸진다.

## AI 예산과 데이터 수집 분리 (2026-09-04)

### 무엇이 문제였나

신호는 **AI 분석이 붙어야만** `ai_signals` 에 저장됐다 — `up_prob` 등이 NOT NULL 이라
AI 없이 저장하려면 없는 확률을 지어내야 했기 때문이다.

그런데 **AI 는 2026-09-03 부터 점수에 들어가지 않는다**(설명 전용). 그 뒤로는
AI 예산(seed = 심볼당 하루 5건)이 **적중률 데이터 수집까지 막는** 부작용만 남았다.

실측이 정확히 일치했다: **21시간 동안 24건.** 상한이 하루 25건이다.

> 점수에 영향도 없는 비용 통제가, 이 서비스의 유일한 실적 데이터를 12배 이상 줄이고 있었다.

### 무엇을 바꿨나

`signal_pipeline` 의 AI 경로와 기록 경로를 **서로 독립**으로 나눴다.

| | 조건 | 빈도 |
| --- | --- | ---: |
| AI 경로 (설명, 돈이 든다) | 게이트 + 봉 차단 + **예산** | seed 기준 하루 25건 |
| 기록 경로 (적중률, 공짜다) | 게이트 + HOLD 아님 + 봉당 한 번 | **하루 300건 안팎** |

- 마이그레이션 007 — 확률·설명 컬럼을 NULL 허용으로. `NULL` = "묻지 않았다",
  값 있음 = "AI 가 답했다". **빈 배열이 아니라 NULL 인 이유**: `[]` 는 "AI 가 근거가
  없다고 답했다"로 읽히는데 실제로는 묻지 않은 것이다. 둘은 다른 사실이고, 나중에
  "AI 가 붙은 신호가 더 정확했나"를 물으려면 구분돼 있어야 한다.
- `SignalEvaluation.is_recordable` — 게이트 통과 + HOLD 아님. **AI 를 부르는지와 무관.**
- Redis `signal:saved:{symbol}:{tf}` — 봉당 한 번만 기록. AI 차단 키와 **따로 둔다**
  (하나로 묶어놨던 것이 문제의 원인이었다).
- 기록 대상이 아니면 슬롯을 잡지 않는다 — 잡아버리면 같은 봉에 나중에 방향이 잡힌
  진짜 신호를 놓친다.

**AI 비용은 그대로다.** seed 예산은 손대지 않았다.

### 무엇을 넓히지 않았나

기록 대상을 **게이트 통과 신호로 한정**했다(중립 구간은 넣지 않는다). 아무거나 넣으면
"신호"의 의미가 사라지고 적중률이 그냥 시장 방향 통계가 된다. HOLD 도 제외한다 —
진입하지 않았으므로 적중 여부를 판정할 수 없다(`strategy/labeling.py`).

## 제품 방향 전환 — 관리자 우선 (2026-09-04 사용자 결정)

> "어차피 현재 사이트가 레드오션이라는 비즈니스모델이고 어차피 경쟁력이 확보가 안 된다면
> 나라도 만족하고 사용할 수 있는 툴로 개발하고 싶어. 내가 먼저 사용해보고 스스로 신뢰도가
> 쌓이면 다른 사람들에게도 자신있게 셀링포인트를 찾을 수 있을 것 같아서."

### 무엇이 바뀌나

- **유저 모드는 지금 상태로 동결한다.** 새 기능을 유저 화면에 붙이지 않는다.
- **관리자 모드에 집중해 개발한다.** 운영자 본인이 쓰는 도구로 먼저 완성한다.
- **법률 제약은 당분간 적용하지 않는다.** `docs/LEGAL.md` 는 **고객에게 보여주는 표현**을
  규율한다. 지금 만드는 화면은 운영자 본인만 보므로 대상이 아니다.
- 나중에 유저에게 열 때 **분기 처리로 노출**한다. 그 시점에 법률 검토를 한다.

### 이 결정이 옳은 이유 (개발 관점)

Step 16·7 이 보여준 것은 **아직 신뢰할 근거가 없다**는 사실이다 — 거래당 기대값이
왕복 비용을 못 넘고, 점수가 품질을 가르지 못한다. 이 상태로 고객 화면을 다듬는 것은
검증되지 않은 것을 파는 준비를 하는 셈이다. **운영자가 먼저 쓰면서 데이터를 쌓고
믿을 만해진 뒤에 여는 순서**가 근거상 맞다.

### 실무 규칙

1. 새 화면·API 는 기본이 **관리자 전용**이다. 관리자가 아니면 403 이 아니라 **404**
   (403 은 그 경로가 존재한다는 것을 알려준다).
2. 유저 노출은 **별도 작업**으로 분리한다 — "나중에 지우기"가 아니라 "나중에 켜기"다.
3. 법률 문구 검토는 **유저 노출 작업의 착수 조건**이지, 관리자 화면의 조건이 아니다.

## 진행 현황

> **일정(날짜)은 두지 않는다.** 지켜지지 않았고, 앞당겨진 폭이 커서 의미를 잃었다.
> **스텝 단위 체크만 한다.** 순서는 날짜가 아니라 **스텝 간 의존**이 정한다.
> 실측·법률검토·전략자문 과정에서 Step 13~18 이 새로 생겼다.

### 완료

| 스텝 | 내용 |
| --- | --- |
| 0 | DB 마이그레이션 (001~006 로컬·서버 적용 완료) |
| 1 | 다중 거래소 수집 + 지표 엔진 |
| 2 | Consensus + RuleEngine + AI 연동 + `ai_signals` 저장 |
| 3 | 유저별 TradingView 웹훅 (발급 API + 수신 엔드포인트) |
| 4 | 백테스팅 (신호검증용 + 업비트 실전용) |
| 6 | REST API + 인증·등급·Rate Limit |
| 9 | 대시보드 (유저 화면 + 관리자 화면 + 차트) |
| 11-a | 뉴스 RSS 수집 |
| 16 | 배점표 재설계 v3 (대칭화 + 중복 제거 + 검증 불가 항목 제외) |
| 14 | 파생 데이터 검증 — **측정 결과 배점·필터 모두 채택하지 않음** (근거는 위 절) |
| 7 | 시그널 성과 추적 스케줄러 (삼중 배리어, 분산 락, systemd 유닛) + 관리자 적중률 화면 |

### 남은 것 — 우선순위

| 순서 | 스텝 | 내용 | 왜 이 순서인가 |
| ---: | --- | --- | --- |
| 1 | **17** | 워크포워드 검증 도구 | **Step 14 를 하고 나서 올라간 순위.** 3구간으로는 "안정적인가"를 판정할 수 없다는 것이 두 번 확인됐다(16 배점표, 14 파생). 구간을 12개로 늘려 같은 질문을 자동 반복하는 도구가 있어야 앞으로의 판단이 싸진다. bybit 가 180일치를 주므로 재료는 있다 |
| 2 | **15** | 김치 프리미엄 | 파생 데이터가 실패했으므로 남은 차별화 후보. **환율 소스 확보가 선행**하고, 17 의 도구로 검증한다 |
| 3 | **5** | 실거래 안전장치·매매 실행 | 스위치·화면은 이미 있다. **지금 실거래로 가면 안 된다** — 비용을 넘는 엣지가 아직 증명되지 않았다 |
| 4 | **8** | 테스트 · 구조화 로깅 · 알림 훅 | 운영 안정화 |
| 5 | **10** | 다국어 (ko/en/ja) | 상승/하락 색이 로케일에 묶여 있어 함께 검수 |
| 6 | **18** | 참고 지표 플러그인 (관리자 토글) | VWAP 계산은 완료, 토글 UI 만 남음. **v3 이 스토캐스틱·CCI·호가를 참고 항목으로 내려놨으므로 토글 대상이 늘었다** |
| 7 | **11-b** | 매크로 시계열·캘린더 | FRED API 키 필요 |
| 8 | **12** | 뉴스 메뉴 + 커뮤니티 | |
| 9 | **13** | 결제·구독 (PG) | **법률 확인이 선행.** 무료 베타 → 검증 → 확인 → 구독 |

### 착수 전 반드시 처리할 것

- **적중률 유저 노출**: 관리자 화면은 만들었다. **유저에게 열 때** 표현 문구를
  법률 검토해야 한다(`docs/LEGAL.md`). 지금은 열지 않는다.
- **Step 5 이전에 반드시**: 거래당 무비용 손익(0.15~0.18%)이 왕복 비용(0.18%)을 넘는 것이
  **Step 7 의 실데이터로** 확인되어야 한다. 확인되기 전에는 실거래를 켜지 않는다.
- **Step 5**: 체결 내역 테이블이 없다. **마이그레이션 005** 가 필요하고,
  관리자 화면의 매매 내역 위젯이 그것을 읽는다.
- **Step 13**: 변호사·금융당국 확인 / 거래소 API 약관 / TradingView 약관 (`docs/LEGAL.md`)

-- ---------------------------------------------------------------------------
-- 008 — 긴 시간 제한 추가 (추세추종 신호를 잴 수 있게)
--
-- 기존 horizon 은 5m~1d 다. 5분봉 룰 엔진에는 맞았지만, 2026-09-04 에 채택한
-- 추세추종은 **평균 23일**을 들고 있는다(BTC 9년 141거래, 절반은 현금).
-- 최장 1일로는 "그 전략이 맞았는가"를 잴 수 없다.
--
-- 1w(7일)와 1M(30일)을 더한다. 기존 값은 지우지 않는다 — 짧은 제한이 대부분
-- TIME_LIMIT 으로 끝난다는 것 자체가 "이 신호는 단기용이 아니다"라는 기록이다.
-- ---------------------------------------------------------------------------
ALTER TABLE `ai_signal_results`
    MODIFY COLUMN `horizon` ENUM('5m', '15m', '1h', '4h', '1d', '1w', '1M') NOT NULL;

"""배점 체계 버전. **적중률 통계가 서로 다른 규칙의 신호를 섞지 않게 하는 기준.**

### 왜 필요한가

배점표를 고치면 그 전후의 신호는 **다른 규칙으로 만들어진 다른 것**이다. 섞어서 평균을
내면 "고쳤더니 나아졌다/나빠졌다"를 영원히 알 수 없고, 대외적으로 말하는 적중률도
무엇의 적중률인지 불분명해진다(`docs/LEGAL.md` — 실적 표기는 근거가 명확해야 한다).

### 왜 날짜로 나누지 않는가

날짜 비교(`created_at < '2026-09-04 08:52:30'`)를 코드 곳곳에 박으면 배점표를 또 고칠 때
새 날짜를 찾아 전부 고쳐야 하고, 한 군데만 빠뜨려도 조용히 섞인다. 그래서 **신호를
만들 때 이 값을 같이 저장**하고(`ai_signals.scoring_version`), 이후 집계는 전부 그
컬럼으로 나눈다. 날짜가 쓰이는 곳은 **기존 행을 소급 분류한 마이그레이션 006 하나뿐**이다.

### 언제 올리는가

**점수가 나오는 방식이 바뀌면 올린다.** 배점표(`rule_engine`), 최종 가중치
(`signal_engine.FINAL_WEIGHTS`), 등급 임계값(`STRONG_THRESHOLD`/`SIGNAL_THRESHOLD`),
방향 임계값(`consensus`), 위험 점수(`risk`) 중 **하나라도** 바뀌면 대상이다.
지표를 새로 계산만 하고 점수에 넣지 않는 것은 대상이 아니다.

    v1  최초 (§3.3 원안, 기준점 없음)
    v2  기준점 40 도입 (2026-09-03) · AI 를 점수에서 제외 (2026-09-03)
    v3  배점표 재설계 — 대칭화 + 중복 제거 + 검증 불가 항목 제외 (Step 16, 2026-09-04)

`v1` 과 `v2` 는 소급 구분하지 않는다. 그 시기 신호는 배포 전 검증 중에 나온 것이라
운영 기록으로 쓰지 않는다 — 나누는 비용만큼의 가치가 없다.
"""

from __future__ import annotations

# 지금 신호를 만드는 배점 체계. `ai_signals.scoring_version` 에 그대로 저장된다.
SCORING_VERSION = "v3"

# 컬럼 길이(VARCHAR(8))를 넘기면 INSERT 가 통째로 실패한다.
MAX_LENGTH = 8
